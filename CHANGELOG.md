# CHANGELOG

All notable framework changes are recorded here. Personal session outcomes
(NAV, P&L, holdings) live in `local/SESSIONS.md`.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) — append-only,
reverse chronological by date-of-event. Categories: `Added`, `Changed`, `Removed`,
`Fixed`, `Process`, `Decisions` (architectural decisions, persona-attributed).

## [Unreleased]

### Process
- Pre-push sanitisation remediation — **DONE** (2026-06-13) ([Proposal 029](proposals/029-pre-push-sanitisation-remediation.md)): `main` sat 31 commits ahead of the PUBLIC `origin/main` with range-diff collisions between investor-private-ledger values and tracked content. Root cause: the S-30 pre-commit hook gates only NEW commits, so the accumulated pre-hook history is what a `git push` would publish. Committed: a pre-PUSH sanitisation hook — `main_prepush` + `range_added_text` in `scripts/hooks/sanitisation_guard.py` (per-commit scan of the outgoing range via the git pre-push stdin protocol; `records=N` active-diagnostic; no-op on a fresh clone) and a new `scripts/hooks/pre-push` shim, installed cloner-portably by a now hook-type-aware `init_workspace.install_hook` (installs pre-commit AND pre-push, installed-AND-OURS asserted). The reconcile test data (`tests/fixtures/portfolio_10col.md` + the inline ledgers in `tests/test_reconcile_ibkr.py`) was rebuilt **fully-synthetic and invariant-preserving** (cost = qty×price + commission, NAV = cash + invested, ISIN-anchored bare-ticker guard) with an added `test_dod6_invariants_through_update_portfolio` exercising the cost/NAV invariants through the `update_portfolio` production entry-point; real-economics quotes were scrubbed from [Proposal 025](proposals/025-reconcile-column-index-foundation-fix.md). All scrub/rebuild ran orchestrator-controlled **in-subprocess** (no private value entered any agent context). 2 isolated-Challenger rounds (R1 15 → R2 9, all absorbed scope-narrowing/correcting); /code-reviewer APPROVE WITH NOTES ×2. Full suite 122 passed / 1 skipped; `find_leaks==0` on the scrubbed files. **Completed across follow-on sessions (separate gates):** item-4 source-driven triage of the 20 net-tree records (3 SCRUB / 13 ALLOWLIST / 4 CLEAN-LOG; net-tree `find_leaks==0`); the PHASE-5 history squash (33 → 1 clean commit) + a local pre-rewrite archive (`archive/pre-029-squash @ 5b9fa90`, retained local-only — never pushed); and the PHASE-7 public push — the clean squash `9c5f86a` fast-forwarded to public `origin/main` (only `HEAD` + `main` published; no archive ref/tags; production pre-push gate over the published range = `records=245`, EXIT 0).

### Changed
- S-27 reconcile Summary canonical contract + S-28 currency-aware flag-only pricing ([Proposal 028](proposals/028-reconcile-summary-contract-currency.md)): fixed two coupled defects in `scripts/reconcile_ibkr.py` surfaced as Challenger L-items during the 025 close-out, both on the sound 10-column ISIN foundation. **S-27** — `_patch_summary`'s five `… (post-fill)` label anchors matched neither the tracked template (0/5) nor the live ledger (3/5; Cash + Invested missed), so a real reconcile produced a silent *partial* patch that the 025 warning misreported as a total no-op. Fix = a canonical Summary contract reconciled across four surfaces (script anchors ↔ `local/templates/PORTFOLIO.template.md` ↔ live ledger ↔ `tests/fixtures/portfolio_10col.md`): anchors renamed to the live labels (`Cash`, `Invested (MTM)`, and the `Equity exposure (% NAV)` wildcard tightened to a literal) so the live ledger — already conformant per the floor audit — needed **no edit**; patching is now section-scoped to `## Portfolio Summary` (precise header `re.match`, line-index splice, no-section → full-no-op) so the generic labels cannot first-occurrence-collide with prose; the 025 warning is reworked into a derived-count graduated message (`updated` / `partially updated — N/M` / `not updated — 0/M`), dropping the hardcoded `/5` and the stale `pending L14`; the template Summary was brought to the canonical CORE-6 (placeholders only). **S-28** — `_fetch_live_prices` fed a native-venue price straight into the base-currency (EUR) unrealised-P&L; fixed **flag-only**: a `fast_info.currency` check prices a mark only when the venue confirms EUR and flags-and-omits any non-base/unknown venue with a loud plain-language warning (skip-don't-misprice) rather than mispricing. FX **conversion** was deferred as unsound while the write-side entry-cost leg is currency-unaware (banked **S-32**). The 026 bare-ticker guard and `import yfinance` guard are byte-preserved. Converged over 3 isolated-Challenger rounds (R1 20 → R2 15 → R3 5 `sound`; the deciding delta R1-L7/L8/L9 drove convert→flag-only, a scope reduction). /code-reviewer APPROVE WITH NOTES (2 NOTEs, both pre-existing, banked to S-32). 6 new tests (label-drift partial/no-op + S-28 currency states through `update_portfolio`); full suite 116 passed / 1 skipped.
- S-23 ISIN-anchored instrument pricing ([Proposal 026](proposals/026-s23-isin-anchored-pricing.md)): killed the bare-ticker valuation trap in `scripts/reconcile_ibkr.py` whereby pricing a holding by its bare exchange-less symbol could resolve to a same-symbol US leveraged fund (a held EU UCITS was valued against a US 3× leveraged namesake — a ~€40 NAV overstatement that hid a position being underwater, caught only by an IBKR cross-check during the 2026-05 War Room). The fix makes the **tracked framework** carry portable instrument identity so every public cloner is protected with `yfinance` alone: `_parse_open_holdings` threads the ledger ISIN (col 1) into each position dict; `_fetch_live_prices` gained a fail-safe **bare-ticker guard** that skips a non-US-ISIN ticker carrying no exchange suffix (loud warning naming the collision + the fix) rather than mis-pricing — a US-ISIN bare ticker is unaffected (yfinance resolves it to its US listing by convention); `_patch_summary` annotates a guard-skipped mark as `(partial: n/m marks)` so a skip can never silently understate aggregate P&L; the tracked `local/templates/PORTFOLIO.template.md` documents the exchange-qualified-ticker + ISIN contract; and the live (untracked) ledger's one bare holding was corrected to its exchange-qualified form (recorded here for audit trail; `_base_symbol` keeps IBKR fill-matching unregressed). The guard is a backstop to the primary qualified-ticker contract — correctly inert on clean live data, synthetic-fixture-tested. 6 new tests in `tests/test_reconcile_ibkr.py` (guard at the real `_fetch_live_prices` entry-point with yfinance faked via `sys.modules`, end-to-end ISIN-thread→guard, partial-mark annotation); full suite 97 passed / 1 skipped. This is the **safety-fix half** of the original S-21+S-23 bundle: two §4b isolated-Challenger rounds (R1=14→R2=7, all claims verified on disk — one structural claim refuted by direct source-read) confined every structural residue to S-21's live-IBKR-extraction surface, so the operator ruled SPLIT — **S-23 shipped, S-21 (`--snapshot`) re-scoped** to a future proposal whose verification gate is the first operator-approved live `--snapshot` run, and **S-29** (global key-free multi-source pricing coverage) banked as the separate coverage build. /code-reviewer APPROVE WITH NOTES (2 NOTE-level; the actionable one — import placement — fixed pre-commit). Supersession/split trail: [Proposal 024](proposals/024-isin-anchored-pricing-ibkr-snapshot.md) → [025](proposals/025-reconcile-column-index-foundation-fix.md) → 026.
- PORTFOLIO partner-name correction + S-21 roadmap item ([Proposal 022](proposals/022-portfolio-partner-name-and-ibkr-snapshot-roadmap.md)): corrected a placeholder partner name ("Sofia") that had been promoted into the live Ownership Tracking table of `local/PORTFOLIO.md` during Session #2 — replaced with the investor-supplied token "PPDC" (lines 45/49/50) and de-risked the line-43 seeding example to generic placeholders (`€300 You, €100 Partner`) so a template example can no longer be mistaken for live data; the principal name "Daniel" and the 50/50 split/amounts are unchanged (the ledger file lives under untracked `local/`, so this fix is recorded here for audit trail but is not in the tracked diff). The tracked change is a new Staged Improvements row **S-21** in `PROGRESS.md`: a read-only IBKR `--snapshot` mode for `scripts/reconcile_ibkr.py` (positions + cash via `ib.positions()`/`ib.accountValues()`) to auto-verify the ledger at War Room start and reconcile at finish — deferred build, triggered on the next edit to that script or first manual ledger-vs-broker divergence. Surfaced while clearing pre-flight for the May 2026 War Room; holdings/cash were verified against live TWS (read-only, port 7496) inline this session. LIGHT/fully-reversible; Isolated-Challenger R1 `sound` (10 L-items: 6 absorbed, 2 resisted with evidence, 2 escalated to user and resolved). Prose carve-out — `/code-reviewer` not required.
- Hypothesis-log lifecycle ([Proposal 021](proposals/021-hypothesis-log-bias-guard-and-closeout.md), supersedes [Proposal 020](proposals/020-warroom-skill-hypothesis-log-read.md)): wired `local/HYPOTHESIS_LOG.md` into the `/war-room` skill (the Phase 4 bullet now names the log) and added two template mechanisms. (1) **Anti-bias** — `brainstorms/_TEMPLATE.md` Phase 4 now instructs that user hypotheses are investigated *alongside*, never in place of, the Signal Generator's own independent search; each reported with a Status (INVESTIGATED / DISMISSED / CARRIED FORWARD) + a one-line Finding, an idea may be found unsupported without forcing a trade, and the orchestrator (a separate Opus agent judging the Sonnet Signal Generator's output) records the verdict and may dismiss a thin "confirm". (2) **Stamp-in-place close-out** — a new `Last Reviewed` column on the Open Ideas table (both the tracked `local/templates/HYPOTHESIS_LOG.template.md` and the live `local/HYPOTHESIS_LOG.md`); after each session the orchestrator appends `<YYYY-MM>:<STATUS>` to every open idea (never overwriting, never moving or deleting), enforced by a Session Close Checklist item and a Protocol Audit row. The `## Investigated` archive section was retired (it duplicated state under stamp-in-place). Pre-existing `local/local/` path typos in `_TEMPLATE.md` (Hypothesis Log Review NOTE + Home-Bias INVESTOR_PROFILE reference) fixed. Design converged across four isolated-Challenger rounds: an initial move-to-archive mechanism generated a structural defect cluster (5-way status-vocabulary incoherence, lossy field-drop, audit/dedup contradiction), resolved by **scope-reduction** (stamp-in-place) rather than added mechanism; the bias guard was likewise leaned onto the existing orchestrator-synthesis layer rather than a heavier two-pass separation (documented as a future upgrade if parroting is observed). /code-reviewer APPROVE WITH NOTES (both notes addressed pre-commit); embedded-directive docs, not prose-exempt.
- S-20 AGENTS.md archetype-line correction + gs_compliance EN/PT example tidy ([Proposal 019](proposals/019-s20-agents-archetype-correction.md)): corrected two `AGENTS.md` Actionability-contract archetype lines made contradictory by Proposals 017/018. The Execution line was re-worded from institutional vocabulary (`venue / routing / TWAP / child-order`) to `virtu_execution`'s retail surface (`order-type / timing / commission-drag / fractional-share`), and `renaissance` was split off the "Quant signal" archetype group onto a new "Overfit detection" line carrying its native review vocabulary (`calibration-window / deflated-Sharpe / purged-k-fold / turnover-cost-ratio`) — re-wording the shared "Quant signal" line was rejected because the IC/decay/capacity vocabulary remains correct for Citadel/point72/AQR/dimensional. Item B: `agents/gs_compliance.yml:63` example-fix string generalised from the Portugal-language literal "EN/PT" to "the investor's jurisdiction-required language(s)" — investor-language-agnostic; `analytical_framework`/`gate_consumption`/`role`/`system_prompt` byte-identical. A user-requested Isolated-Challenger pass (R1 verdict `flawed`, L1–L10, structural L1/L2/L3) was absorbed pre-commit — the `dimensional_factor_backtester` loose grouping under "Quant signal" is recorded as an explicit out-of-scope follow-up rather than scope-crept. /code-reviewer APPROVE on the persona file; AGENTS.md prose-exempt under the carve-out.
- S-18 `virtu_execution` persona re-architecture ([Proposal 018](proposals/018-s18-virtu-execution-rearchitecture.md)): standalone successor to the SUPERSEDED [Proposal 016](proposals/016-s17-s18-persona-quality-lift.md) S-17+S-18 bundle. `agents/virtu_execution.yml` re-architected from a fictional institutional execution-algorithm developer into a retail execution-quality analyst for the desk's actual investor: free-prose `output_format` replaced with a structured 5-block decision-rule schema (order_context, order_type_rule, timing_rule, cost_efficiency, execution_quality_rationale); `system_prompt` re-scoped to the retail-DCA context with four behavioural constraints; `capabilities` rewritten to the retail surface; `review_prompt` reframed to four retail-execution checks (Order-Type Discipline, Timing, Commission Drag & Consolidation, Execution Quality & Fractional Deployment); `name`/`role` changed; `firm`/`gate_consumption`/`analytical_framework` preserved byte-identical; pre-rewrite persona archived at `proposals/018-backups/virtu_execution.original.yml`. Per an APPROVE-WITH-AMENDMENTS de-hardcode decision, investor literals (contribution amount, broker name) are not hardcoded — the persona reads them from `local/INVESTOR_PROFILE.md` at runtime, consistent with the repo's `local/`-segregation convention. Byte-verbatim eval battery (DoD #11–#19): median-of-3 composite — rewrite 31, clearing the absolute 30 threshold, measured against the original re-measured on the same retail scenarios at 25 gate-applied / 27 pre-gate; the planted-bug cap on the original is legitimate (a genuine in-domain miss — the institutional persona's review checks carry no commission-consolidation lens; corroborated cap-free by both cold reviewers). The 31 is NOT directly comparable to the 2026-05-15 baseline of 29 — different scenario basis (retail vs institutional). Zero-regression gate held; D1 and D6 both improved (diagnosis confirmed); dual cold reviewer both returned GENUINE IMPROVEMENT; pair-diff 3→4 vs jane_street_mm and 4→4 vs millennium_live_trading; both-branch dry-run discriminates the monthly and consolidated cases. The eval row (composite 31, dated 2026-05-21) was appended to `~/.claude/persona_eval_history.csv`; the de-hardcode amendment scrubbed investor literals from the proposal body as a Commit-2 prerequisite. /code-reviewer APPROVE WITH NOTES on the persona; documentation-only on the docs commit.
- S-17 `renaissance_backtesting` persona lift ([Proposal 017](proposals/017-s17-renaissance-backtesting-quality-lift.md)): standalone successor to the SUPERSEDED [Proposal 016](proposals/016-s17-s18-persona-quality-lift.md) S-17+S-18 bundle (the virtu half was structurally unconverged across three isolated-challenger rounds; the bundle was split, S-18 deferred to a future Proposal 018). `agents/renaissance_backtesting.yml` rewritten: free-prose `output_format` replaced with a structured 7-block backtest-validity schema (backtest_subject, rolling_sharpe_stability, out_of_sample, transaction_cost_realism, turnover_cost_ratio, regime_conditioned_hit_rate, overfitting_verdict); `system_prompt` mandates the schema, carries `frictionless_flag`→HIGH-severity and `overfitting_verdict`/`stability_verdict` coherence rules, and drops the legacy "full Python code" output mandate; `review_prompt` reframed around a calibration-window-overfit adversarial frame with 6 numbered checks; vocabulary discipline added against dimensional/citadel/pimco territory. Critical-pattern fields preserved verbatim; pre-rewrite persona archived at `proposals/017-backups/renaissance_backtesting.original.yml`. Byte-verbatim eval battery: median-of-3 composite 28→33 — 5 of 7 dimensions lifted +1, D3 (Actionability) and D6 (Environmental Adaptation) flat (not regressed); zero-regression gate held; pair-diff vs dimensional 3→4 and vs citadel 4→4; dual cold reviewer PASS (every dimension ≥ original, ×2); both-branch dry-run discriminates overfit→likely-overfit / sound→real-alpha. An initial eval pass embedded condensed persona content in the sub-agent dispatches; a pre-commit fidelity check caught it and the full 8-dispatch battery was re-run with the persona files embedded byte-verbatim — the verbatim composite (33) is one point below the condensed run's reading (34), confirming the condensation was materially optimistic on D6.
- S-19 instrument-compliance guard carry-forward ([Proposal 014](proposals/014-s19-compliance-guard-pimco-deshaw.md)): applied the jurisdiction-driven UCITS/broker-availability compliance guard (originally on `citadel_alpha.yml` via Proposal 010 §Amendment 2) to two more signal-pool personas. `agents/pimco_curve_strategist.yml` received a structured-schema treatment (`compliance_uncertain: boolean` field in `output_format`; `bond_etf_allocation`-targeted "Instrument compliance constraint" paragraph in `system_prompt` above the INVESTOR PROFILE trailer). `agents/de_shaw_statarb.yml` received a memo-header convention treatment (free-prose `output_format` unchanged; `system_prompt` paragraph requires an unconditional `compliance_uncertain: [true|false]` header on every memo for grep-able Phase 8 enforcement). Schema-vs-memo divergence is deliberate — restructuring de_shaw's `output_format` is a separate HEAVY proposal. /code-reviewer Note 2 (header conditionality) addressed inline before commit; Note 1 (schema-as-annotation convention) deferred as documentation-shape, not defect.
- S-16 closing-example polish ([Proposal 012](proposals/012-citadel-twosigma-example-trio-polish.md)): replaced the generic risk-domain example trio (`"reduce size 8%→4%", "stop at 88.00", "block trade until VIX<22"`) — which had survived the Proposal 010 option-b amendment in two persona files — with archetype-native trios. `agents/citadel_alpha.yml:108` now carries Citadel signal-validation examples (in-sample IC threshold, OOS t-stat, signal capacity vs AUM bottleneck). `agents/two_sigma_risk.yml:62` now carries Two-Sigma portfolio-risk examples anchored on its capability list (Kelly stake, 30d cross-position correlation, 99% VaR contribution). Pre-flight enumeration grep across all 17 personas confirmed no other file carried the generic trio and no vocabulary collision exists for any of the 10 new phrasings. Closes the Proposal 010 audit brief §H.1 deferred polish and the parallel bleed in two_sigma_risk surfaced by 2026-05-16 pre-flight.

### Added
- **S-25d per-series ungated core-macro visibility** ([Proposal 033](proposals/033-s25d-per-series-ungated-render.md)): `gate_eval.render_table` now appends a per-series note block listing each fetched-but-ungated core-macro series (US CPI / US unemployment / ECB deposit rate / euro-area HICP) with its human-readable label, Data-Confidence tier and staleness age — so a War Room operator seeing `Data_Confidence_Tier: RED/AMBER` can see WHICH ungated series drove it (completes the S-25/S-25c surfacing story). **Visibility-only — no re-tiering**: the ungated fold computes each series' tier once and reuses the same scalar value for both the aggregate `data_staleness_tiers` input (unchanged) and a new JSON-serialisable `GateReport.data_confidence_series` field; `TestUngatedFoldIn` confirms the aggregate is byte-identical. The block is a **non-pipe** form (lines start `**`/`- `, bare tier tokens) so `parity_check.extract_prose_tiers` stays blind to it — the mandatory choice because a pipe row labelled "ECB deposit rate" (DFR) would collide with `_PROSE_LABEL_TO_GATE_ID`'s `ecb` mapping and inject a spurious gate tier; block lines sorted by `series_id` for canonical output. `.claude/commands/war-room/SKILL.md` Step B.5 paste wording clarified (full output, not just the table). Isolated-Challenger R1 (13, flawed) → R2 (11, flawed) → R3 (sound) — all L-items byte-verified; key absorptions: corrected the parity inertness oracle (`extract(table+block)==extract(table-alone)`, since a count-of-8 would mask a same-key overwrite), the `extract_prose_tiers(Path)` test mechanism, and a negative assertion that a gated series is excluded from the capture. `/code-reviewer` APPROVE WITH NOTES (3 NOTEs, non-blocking). **Parity-map fix (operator-approved scope expansion, 2026-06-22):** `scripts/data/parity_check.py` gained `"us_payrolls": "us_payrolls"` in `_PROSE_LABEL_TO_GATE_ID` — `render_table` emits gate-**ids** as col-0 labels (`us_payrolls`, underscore) but the map only had the human aliases `"us payrolls"`/`"payrolls"`, so the **verbatim** render parsed to 7 gates, not 8 (pre-existing; S-25d's block is genuinely inert). A new real-render test (`test_real_render_with_block_round_trips_to_exactly_eight`) parses the actual `render_table` bytes + populated ungated block and asserts exactly the 8 deployment gate-ids — the human-label fixture is retained only as an alias-path check. Fresh ≥L1 Challenger pass on the delta → sound; its L4 (the fix patches the `us_payrolls` instance, not the class) was closed by a **class-guard test** that derives the deployment-gate set from `config/gates.yml` (authoritative — covers any future gate) and asserts every gate-id self-maps in `_PROSE_LABEL_TO_GATE_ID`, with a non-vacuity guard. 12 new tests (5 `test_gate_eval.py` + 7 `test_parity_check.py`); full suite **175 passed / 1 skipped** (was 163/1). Runtime-obs: `gate_eval --session 2026-06 --format markdown` renders all 4 ungated series (CPI 18d / DFR 18d / UNRATE 20d / HICP 50d, all GREEN); the verbatim render now parses to exactly 8 gates. Completes the S-22–S-25 data-layer arc.
- **S-25c euro-area HICP source migration** ([Proposal 032](proposals/032-s25c-eurostat-hicp-provider.md)): replaced the dead ECB euro-area HICP key `ICP.M.U2.N.000000.4.ANR` (stale at source — latest obs `2025-12`, `startPeriod=2026-01` empty across all ICP reference areas after a 21st euro member joined in 2026 → ECB ICP `U2` migration lag) with the currently-updating Eurostat `ei_cphi_m` all-items HICP annual rate via a **new `EurostatProvider`** (JSON-stat). Same signal — all-items HICP YoY %, the statistic ECB ICP re-disseminates from Eurostat — not a substitute. `scripts/data/providers/eurostat.py` mirrors `EcbProvider`'s class structure (shared `HttpClient`, `_SERIES_REGISTRY` alias→`(dataset, dim-filter, units)`, `known_series()`); the `_parse_jsonstat` parser selects the value at the highest `dimension.time.category.index` ordinal and raises `HttpError` (loud → CLI abort) if that ordinal is absent/null in the sparse JSON-stat `value` object — never a silently-older value under the latest period's label. `cli.py _build_providers` gained an `EUROSTAT` branch (ECB-shape: alias validated against `known_series()`, else `unresolvable → sys.exit`). **Thresholds recalibrated to `amber=55 / red=90`** (operator-confirmed, Risk-Officer-ratified): with first-of-month vintage anchoring + HICP's ~17d publication lag the latest legitimate print ages ~47→75d across a monthly cycle, so 55/90 reads GREEN when genuinely fresh, AMBER as the next print becomes due, and reserves RED for a genuinely stalled feed — 45/60 would have thrown the legitimately-latest print into the RED action tier for ~2 weeks of every cycle (cry-wolf; never validated live because the dead key was always-RED). The S-25 source-agnostic ungated fold + cautious-fail invariant are preserved (a stale/None/unparseable HICP still reads AMBER+, never silent GREEN). SHA cascade: the swap changed the canonical `compute_gates_content_sha` to `c437a02546412205c66a8df0bcc26864a7db04533e323514c6f5af85568cec96` (independently recomputed against the edited bytes); `backtesting/REPLAY_DELTA.md`'s current-SHA line replaced **in place** (the "at replay" `89ff983d…` preserved) and `proposals/003` got an append-only entry, so `TestArtefactSync` stays green; the prior `a8d90add…` is preserved in the 003/031 Status Logs. A CI ghost-contract guard (`test_eurostat_aliases_registered`) asserts every gates.yml EUROSTAT alias is registered in the provider (the runtime `_build_providers` check never fires in CI); a removal guard asserts the dead ICP key is gone from gates.yml + `ecb.known_series()`. Isolated-Challenger R1 (13, FLAWED) → R2 (7, FLAWED, descending) → R3 (1, sound) — all L-items byte-verified; the R2 catch (the `test_live_smoke.py` `else ecb` source-routing fallthrough — a missed consumer) and the structural registry-membership gap were absorbed scope-correcting. `/code-reviewer` APPROVE WITH NOTES (one NOTE — JSON-stat dense-array form — folded into the parser docstring; Eurostat returns object-form, which still aborts loudly). Runtime-obs (run in the steering session via the project CLI; FRED key auto-loaded from `.env` by `load_dotenv`, never in agent context): live `EUROSTAT ei_cphi_m.TOTAL.RT12.EA = 3.2`, vintage `2026-05`, HICP ungated contribution **GREEN** at 50d < 55 (was RED-by-staleness ~169d); every fetched-data contributor GREEN; overall `Data_Confidence_Tier` RED driven entirely by the 4 still-unwired manual gates (hormuz/ecb/tariff/stoxx600 — pre-existing, out of scope). 13 new tests; full suite 163 passed / 1 skipped (was 152/1). **S-25d** (per-series visible stale-row in `render_table`) remains the open follow-up.
- **S-24 brent fetch wire-up (PARTIAL) + S-25 stale-core-macro surfacing (DONE)** ([Proposal 031](proposals/031-s24-brent-s25-stale-core-macro.md)): pre-War-Room-#4 data-layer fixes. **S-24 (brent leg)** — `DCOILBRENTEU` added to `config/gates.yml` → `data_staleness.series` (`{source: FRED, amber_age_days: 5, red_age_days: 10}`, matching the pre-existing `_GATE_STALENESS["brent"]`) plus a `fred.py _SERIES_META` entry labelling units `USD_per_bbl` (else `_DEFAULT_META` silently labels it `index`). The fetch loop iterates `data_staleness.series`, so brent was never fetched → `unavailable → RED-by-omission`; it now scores its real tier from a live value (runtime-verified: `DCOILBRENTEU=97.46`, vintage `2026-06-11`, AMBER). S-24 remains **PARTIAL** — the equity-vs-50wMA gate (new equity-provider archetype) and the 3 categorical/geopolitical gates (hormuz/ecb/tariff, needing a structured manual-input slot) are deferred to own proposals. **S-25 (DONE)** — stale ungated core-macro series now drive the `Data_Confidence_Tier` headline (aggregate surfacing); per-series visible stale-row in `render_table` deferred as **S-25d**. `gate_eval.evaluate_gates` folds the fetched-but-ungated core set (`(snapshot.series ∩ data_staleness.series) − gated`) into `Data_Confidence_Tier` generically (thresholds sourced from `data_staleness.series`, not `_GATE_STALENESS`); a new `_parse_vintage_to_date` parses ECB monthly `YYYY-MM` vintages (anchored first-of-month, the conservative direction) so a months-stale HICP becomes measurable; and a new `_data_confidence_contribution` closes the cautious-fail gap — a fetched (`data_source=="live"`) series whose vintage cannot be parsed reads AMBER (cautious), never silent GREEN, while manual `cached`+None gates stay fresh-by-design (GREEN) and a `cached`-but-stale gate reads its real tier. The gated path is unchanged (keeps `_GATE_STALENESS` as its threshold source; gated parse site untouched). **Live probe root-cause (recorded in the proposal):** the euro-area HICP key `ICP.M.U2.N.000000.4.ANR` is genuinely months-stale at source (a same-day live probe returns observations ending `2025-12`; both ANR/INX suffixes confirm; `startPeriod=2026-01` empty) — correcting the key is banked as **S-25c**. SHA cascade: adding the series changed the canonical `compute_gates_content_sha` to `a8d90addea00c75cfe114859707353b3483a24e1d1220321cbc84a744ef5e542` (recomputed against the edited bytes); `backtesting/REPLAY_DELTA.md` gained a separate current-SHA note (the "at replay" SHA preserved) and `proposals/003` an append-only Status-Log cross-ref, so `TestArtefactSync` passes; a new `_GATE_STALENESS↔gates.yml` consistency test guards the dual threshold-source against drift; `DCOILBRENTEU` pinned in `test_gates_schema.REQUIRED_STALENESS_SERIES`. Isolated-Challenger R1 (FLAWED, 14 L-items — all byte-verified then absorbed scope-narrowing/correcting, resisted with evidence, or accepted-as-constraint; the gated-parse-site mutation was dropped from scope, L2). `/code-reviewer` APPROVE WITH NOTES (3 NOTEs; the one logic NOTE — None-valued thresholds → TypeError in the ungated fold — is unreachable under the `test_gates_schema` int-threshold guard and fails loud, not silent). `docs/RISK_FRAMEWORK.md` Evaluator Failure Protocol gained a "stale ungated core series" row (Strategy-Doc Sync). Runtime obs both DoD#7 halves (live brent fetch + stale-HICP drives Data_Confidence AMBER→RED via `evaluate_gates`, GREEN-leg counterfactual confirmed). 11 new tests; full suite 152 passed / 1 skipped (was 141/1).
- **S-31 sanitisation-guard precision + corpus hardening** ([Proposal 030](proposals/030-s31-sanitisation-guard-precision-corpus.md)): four reconciled follow-ups to the S-30 guard, every narrowing paired with a real-leak-STILL-blocks test (cardinal: detection must not weaken). **(1) Precision (tickers-only)** — `find_leaks` now branches on `record["kind"]`: a **ticker** uses an alpha-AND-digit boundary (`(?<![A-Za-z0-9])…(?![A-Za-z0-9])`) to drop the coincidental-substring over-block; **ISIN and numeric stay digit-only** (a 12-char ISIN is high-value → keep over-blocking; a value glued to a currency code like `5142EUR` is the highest-stakes leak class → must still match). Operator scoped this to tickers-only after a Challenger pass showed an ISIN/ticker alpha-boundary under-blocks a glued-no-separator real token; the ticker glued-no-separator case (`FAKE.DENotes`) is a named, operator-accepted residual. **(2) Fail-CLOSED** — new `GitInvocationError` + `LedgerReadError`: `staged_added_text`/`range_added_text` raise on a git error (a legitimately empty/delete-only diff still returns `""` → PASSES), and `extract_private_literals` raises `LedgerReadError` on `OSError`; both entry-points (`main`/`main_prepush`) catch → a clean BLOCK + non-zero exit rather than the prior silent fail-open. `main_prepush` also fails closed on a malformed pre-push stdin line, an unresolvable no-stdin/no-origin range, a rev-list-fallback git error, and a per-commit `git show` error (rev-list's own except preserved — never weakened to a crash). A non-UTF-8 ledger (e.g. a Latin-1 broker-statement paste) is **best-effort latin-1 decoded** (operator-endorsed, replacing a directed loud-skip that a Challenger showed was a real under-block) so its ASCII-shaped values are still harvested and still block, with a loud warning — neither silent under-block nor DoS. **(3) Doc-discipline** — a named convention residual (placeholders + short SHAs in tracked docs), no code. **(4) Corpus hardening** — `extract_private_literals` excludes process/meta noise via a convention-glob (`_is_process_noise`: `.steering-`/`.issues-scratch` infixes, `INDEX.md`, `_draft` stems) so a future steering log auto-excludes (closing the self-poisoning root cause); a no-swallow test guards every real ledger source; the `_tracked_paths` (templates) subtraction is preserved. Subsumption gate (4iii): the three excluded files with non-zero non-subsumed counts were operator-attested benign in a separate terminal (PIN-1 held — values never entered agent/test context); `INDEX.md` + the working-prompt draft excluded without a gate (non-subsumed count == 0, trivially safe). Design converged over an isolated-Challenger R1 (16 L-items, all byte-verified) + an operator-mandated R2 delta-pass on the L3 read-error addition (10 dL-items). /code-reviewer APPROVE WITH NOTES (NOTE 1 — malformed-stdin message — fixed; NOTE 2 — `INDEX.md` exact-match — accepted as named residual). Verified through the installed `.git/hooks/pre-commit` (real-leak BLOCK / excluded-value PASS / glued-ticker PASS / latin-1 still-BLOCKS) plus a clean self-commit dogfood (all staged files clean vs the live ledger). 19 new tests in `tests/test_sanitisation_guard.py`; full suite 141 passed / 1 skipped (was 122/1).
- **S-30 mechanical pre-commit sanitisation guard** ([Proposal 027](proposals/027-s30-precommit-sanitisation-guard.md)): forward-prevention for the sanitisation rule that had been noted-but-unenforced since Proposal 018 and recurred N≥2 (the Proposal 026 test hardcoded real qty/price/cost + live NAV into a public tracked file, caught by steering not tooling). A tracked `scripts/hooks/pre-commit` shim — installed cloner-portably into `.git/hooks/` by a new `init_workspace.install_hook()` (idempotent; never clobbers a foreign hook; zero new dependency) — reads the gitignored `local/` ledger at runtime (private literals never enter any agent context, PIN 5) and **blocks the commit** when an ISIN, exchange-qualified ticker, or ledger-numeric value appears in the staged tracked diff. Numeric matching uses **literal-side variant-enumeration** (each private value rendered in plain / comma-grouped-US / EU-locale forms **from the original token string**, so a significant trailing zero is preserved) + a **raw-diff digit-lookaround search** (`(?<!\d)…(?!\d)` with `re.escape`), which eliminates the two-sided-normalisation symmetry class at the root and makes the failure direction **fail-CLOSED** — a rare over-block is recoverable via the parsed-canonical allowlist (`config/sanitisation_allowlist.yml`, PIN-3 hashing) or `git commit --no-verify`. The guard no-ops gracefully for a fresh cloner (absent/empty `local/`) and is right-sized as a mechanical safety net (NOT a perfect redactor) with named, bounded, fail-closed residuals (free-text PII, `.env` secrets, `.html`-only values, space-separated / scientific numbers, sub-floor ≤3-digit integers). Design converged across 4 isolated-Challenger rounds: a two-sided canonical-normaliser introduced a symmetry spiral (R4 returned 5 structural items, up from 2 — date-adjacency, EU-locale) and was **scope-reduced** (Amendment 1) to the one-sided enumeration rather than patched. Two follow-on amendments were forced by self-gate dogfooding (the installed hook run against the real ledger): **Amendment 2** added a diff-side ISO-date exclusion (a ledger bare-year was matching governance-doc dates) reusing the existing `DATE_RE` — ledger extraction unchanged, so the R2-L3 fail-open stays closed — plus dropped a literal head-SHA from the docs; **Amendment 3** raised the decimal floor to ≥3 significant figures (a tiny `1.3`-class ledger decimal was harvested and its 3-char EU-variant collided ubiquitously), keeping the verified leak class (`63.4` qty, `63.41`, `5142.30`, ≥4-digit NAVs) and naming the ≤2-sig-fig decimal as a bounded residual. The dogfood also caught a **real** Challenger-surfaced ledger value mistakenly carried into the guard docstring (scrubbed before commit). Block reports name kind+file only, never the value (PIN 1); all tests use synthetic literals only (PIN 2). /code-reviewer APPROVE WITH NOTES (3 NOTEs, all fail-closed/non-blocking — banked as S-31). Verified through the real `.git/hooks/pre-commit` with 5 runtime observations (BLOCK value-redacted / PASS / genuine-ALLOW / fresh-cloner no-op / 026-shape across plain+US+EU + trailing-zero + grouped-integer, with substring word-boundary false-positives correctly passing) **and a clean self-gate** (the guard passes its own full commit with no `--no-verify`). 13 new tests in `tests/test_sanitisation_guard.py`; full suite 110 passed / 1 skipped.
- Data-layer roadmap items **S-22–S-25** (`PROGRESS.md`), surfaced during the 2026-05 War Room (#3, first 0-trade HOLD session): (S-22) the macro-fetch HTTP cache has no TTL and no `--no-cache` flag, so a live fetch can silently serve stale cached responses → risk of a false `Data_Confidence_Tier` RED; (S-23) anchor instrument pricing/verification on ISIN via the IBKR API and retire bare-ticker yfinance lookups — a bare exchange-less ticker collided with a same-symbol US leveraged fund during valuation (extends S-21); (S-24) wire the 5 manual deployment gates (plus a central-bank meeting-calendar feed) into the pipeline so they are not scored RED-by-omission; (S-25) surface stale/materially-wrong core-macro series (observed: a months-stale euro-area inflation series) in the `gate_eval` headline rather than burying it in per-series staleness. Roadmap-only (deferred builds); no framework behaviour changed this session. Prose carve-out — `/code-reviewer` not required.
- Governance Phase B: implementation-agent sub-agent, skill-config.yaml for Investments & Pilates, review_prompt adversarial field on all 17 War Room agents, prompt-selection-by-phase in /war-room.
- S-15 ([Proposal 010](proposals/010-s15-s16-persona-quality-lift.md)): `citadel_alpha.yml` rewritten with structured 7-field `output_format` schema (signal_proposal, information_coefficient, decay_curve, regime_dependence, capacity_estimate, long_only_etf_translation, compliance_uncertain), Citadel-specific "assume the signal is data-mined" adversarial review frame with 4 quantitative checks, jurisdiction-driven instrument compliance guard linked to INVESTOR_PROFILE.md. Median-of-3 composite 14→27 (+13).

### Changed
- S-14 ([Proposal 009](proposals/009-s14-redundant-agent-pairs.md)): Resolved two redundant persona pairs from Phase D /persona-eval. `bridgewater_macro` → `pimco_curve_strategist` (new firm, `yield-curve-regime` framework, `duration_positioning_recommendation` output schema); `de_shaw_statarb` rewritten with `structural` framework (share-class selection, cross-listed, tracking-difference) for long-only ETF cadence. Median pair-differentiation: 2/5 → 4/5 on both. Phase 7 panel slot #8 preserved.
- S-16 ([Proposal 010](proposals/010-s15-s16-persona-quality-lift.md)): Uniform D3 actionability contract applied to all 17 personas' `review_prompt` blocks — HIGH/MEDIUM/LOW severity tag + specific fix + priority ordering (irreversibles first). Each persona ships archetype-native fix examples (macro→gate/regime, risk→stops/VaR, execution→TWAP/venue, signal→IC/decay, compliance→KID/MiFID, etc.) to prevent cross-domain bleed. Stratified-sample eval (5 personas): median-of-3 5/5 PASS, cold-reviewer 5/5 PASS, Phase 8 dry-run 5/5 PASS, bleed-close re-eval 3/3 PASS. virtu_execution received Real-time-Execution-Stop harmonisation (CRITICAL/HIGH/MEDIUM → HIGH/MEDIUM/LOW project standard while preserving its effort-estimate language < 1 day / 1-3 days / > 1 week).
- War Room framework enum (`.claude/commands/war-room/SKILL.md`) extended with `yield-curve-regime` and `structural`.

### Fixed
- `reconcile_ibkr.py` 10-column ISIN-schema foundation fix ([Proposal 025](proposals/025-reconcile-column-index-foundation-fix.md), successor to SUPERSEDED [Proposal 024](proposals/024-isin-anchored-pricing-ibkr-snapshot.md)): the post-execution reconcile script parsed and patched the ledger's **Current Holdings** table on a **9-column** layout, but that table has carried a 10th column (**ISIN at index 1**) since the initial commit — the insert shifted every later column right by one. `_parse_open_holdings` tested `cols[8]` (Stop-Loss, never `"OPEN"`) → returned `[]` for every position; `_patch_holdings_row` matched `"PENDING FILL" in cols[8]` → a silent no-op (the live ledger was therefore intact, **not** corrupted — verified). Corrected both functions to the verified map (`[0]Ticker [1]ISIN [2]Name [3]EntryDate [4]Quantity [5]EntryPrice [6]EntryCost [7]Strategy [8]StopLoss [9]CurrentStatus`); the cost-cell `€`/comma sanitisation moves with the index; an inline column-count comment records the contract. The other two enumerated functions (`_pending_tickers`, `_patch_trade_row` — the latter targets the 9-column Trade-History table, no ISIN) were audited **verified-correct and left untouched** (surgical scope). Restoring the parser **re-activates** `update_portfolio`'s dormant Summary branch, so the now-active `_patch_summary` no-op (its regex labels match neither the tracked template nor the live ledger — banked as **S-27**, immediate-next) was made **loud**: `_patch_summary` switched to `re.subn` miss-counting and returns `(text, n_missed)`; `update_portfolio` appends a "Summary not updated — label mismatch … pending L14" warning, printed by `main`. New tracked `tests/fixtures/portfolio_10col.md` (Holdings header byte-verbatim from the live ledger) + `tests/test_reconcile_ibkr.py` (4 tests; `pytest.importorskip("ib_insync")`; the end-to-end test traverses the production `update_portfolio` entry-point and asserts the loud warning fires) — full suite 91 passed / 1 skipped. Isolated-Challenger ran on both 024 (`flawed`, structural FATAL → foundation-first reset) and 025 (`flawed`, 15 L-items — 10 absorbed incl. the operator-authorised loud-guard, 5 resisted as pre-existing/out-of-surgical-scope, no scope expansion); /code-reviewer APPROVE WITH NOTES (1 NOTE, folded). Banked: **S-27** (Summary-label 3-way reconciliation, prioritised), **S-28** (venue-currency-aware pricing, dormant). S-21 (`--snapshot`) and S-23 (ISIN-anchored pricing) remain open, re-scoped to a clean proposal on this sound base.
- S-22 macro HTTP-cache TTL + `--no-cache` ([Proposal 023](proposals/023-s22-http-cache-ttl.md)): the data-layer on-disk HTTP cache (`scripts/data/http_client.py`) had no expiry — `get()` returned a cached response unconditionally once the file existed, so a "live" `fetch` could silently serve stale data (War Room #3 served an 18-day-stale VIX/EUR-USD/HICP vintage that would have tripped a **false session `Data_Confidence` RED**; only a manual cache-dir wipe forced live data). Added a per-series freshness gate: `get()` gains a keyword-only `max_age_days`, and `cli._fetch_all` threads each series' **`amber_age_days`** (from `config/gates.yml`) down through the now keyword-only-no-default `DataProvider.fetch` signature (closing a silent `None`-default footgun at the macro boundary). Over-age entries are re-fetched on the normal path. **Bypass-on-expiry, never evict** — an expired entry stays on disk and is returned as the tagged-STALE Data Degradation Protocol step-2 fallback (`docs/RISK_FRAMEWORK.md:286`) if the re-fetch fails; the 429-over-cap abort was rerouted (`last_exc`+`break`, `L19 (rev L5)`) to converge on that same post-loop fallback decision (a back-off outage is a degraded-mode condition, uniform with the 500/RequestException paths). Amber (not red) keying chosen so cache-age contribution stays inside the GREEN band where `(red − amber) ≥ publication_lag`. A manual `--no-cache` flag on `fetch` (reuses the existing `skip_cache` plumbing) forces a fully-live fetch and retires the manual `find … -delete` War Room pre-flight; it also disables the DDP fallback (raises on outage), documented in the flag help. New `tests/test_http_client.py` (5 tests traversing the real `get()` path with monkeypatched clock + network: fresh-serve-no-network, over-age-refetch, RequestException→STALE-fallback, 429-cap→STALE-fallback, skip_cache→raises); 11 tests pass total. Isolated-Challenger R1 (flawed, 11 L-items — 9 absorbed, L2+L5 operator-authorised scope expansions) + bounded delta-R3 (flawed, 10 L-items — converged-cosmetic-residual, no further expansion); /code-reviewer APPROVE WITH NOTES (4 NOTE-level, none gating; finding #4 — pre-existing non-atomic cache write — logged as future hardening **S-26**).
- risk_guardian.yml escalation_rules quote-wrapped so yaml.safe_load succeeds (two pre-existing colon-space defects).
- `.env` auto-load via `python-dotenv` ([Proposal 011](proposals/011-dotenv-auto-load.md)): `scripts/data/__init__.py` and `scripts/init_workspace.py` now call `load_dotenv()` at module top; editing `.env` is sufficient with no manual `source`/`export` step. `override=False` default preserves shell-set env vars. Empty-key fail-fast blockers at `cli.py:100-103` and `gate_eval.py:511` preserved verbatim. Closes the "edited `.env` but forgot to source" foot-gun discovered when FRED smoke had been silently blocked since `.env` was committed.
- DoD #10b live smoke (data-layer-integration): `python3 -m scripts.data fetch --session 2026-05 && python3 -m scripts.data gate_eval --session 2026-05` ran clean on 2026-05-15 — 7 live observations (4 FRED + 3 ECB), snapshot `sha256:83207ac933d7…`. Rollback clock (2026-06-20) retracted.

### Decisions
- (Macro pool, PIMCO Curve Strategist) Replaced Bridgewater Macro with PIMCO Curve Strategist — adopts ACM/Kim-Wright term-premium decomposition and carry-and-roll-down lens, producing duration-positioning recommendations (not regime classifications). Rationale: removes brand+output-type overlap with `macro_strategist` that was driving 2/5 pair differentiation despite genuinely distinct yield-curve mechanics.
- (Signal pool, D.E. Shaw StructArb) Reframed D.E. Shaw from statistical arbitrage to structural arbitrage. Rationale: stat-arb requires short legs and sub-daily monitoring, neither available to a long-only monthly ETF DCA investor. Structural lens (share-class, cross-listed, tracking-difference) fits the actual decision surface and connects to Phase 5 Instrument Verification.
- (Signal pool, Citadel Alpha) Replaced free-prose `output_format` with a structured 7-field schema and Citadel-specific data-mining adversarial frame. Rationale: closes the static-label dominance gap flagged by Proposal 009 cold reviewers — same-firm-brand continuity is preserved but output-schema name/structure and review-prompt framing now break adequately. Pair-diff vs point72/dimensional/aqr now 4/4/4 (target ≥4).
- (Library-wide, S-16) Adopted uniform actionability contract with per-persona archetype-native fix-example vocabularies. Rationale: the alternative (single shared example set) caused observable cross-domain bleed in dry-run (compliance and ML personas producing sizing fixes). Per-persona examples retain the uniformity principle on the contract while removing the bleed vector — confirmed by bleed-close re-eval 3/3 PASS.

## [2026-04-27]

### Removed
- `CLAUDE.md` §Skills table — deleted three phantom Local skill rows (`/audit`, `/backtest`, `/risk-check`) that advertised skills not present on disk; build-out deferred as Staged Improvement S-13 (Proposal 008).

### Changed
- `PROGRESS.md` §Staged Improvements — added S-13 logging the deferred build-out of `/audit`, `/backtest`, `/risk-check` with trigger conditions and portability note.
- `README.md` §Requirements — added Windows-only Git for Windows prerequisite with explanation that Claude Code requires `bash.exe` as its shell (not for version control).
- `README.md` §Quick Start — added Windows callout blockquote above the clone command block.
- `proposals/README.md` — rows 006, 007, 008 added to index.

### Added
- `proposals/006-windows-git-prereq-readme.md` — proposal file for Windows Git prerequisite documentation update.
- `proposals/007-war-room-first-run-guard.md` — proposal file for first-run pre-flight guard.

### Fixed
- `.claude/commands/war-room/SKILL.md` — added Pre-Flight: First-Run Detection block before Phase 0; skill now halts with an onboarding redirect when `local/INVESTOR_PROFILE.md` is missing or empty (zero-byte), instead of silently stalling at Step A (Proposal 007).

### Process
- [Proposal 006](proposals/006-windows-git-prereq-readme.md) DONE — LIGHT doc change; governance 2/2; no code modified.

## [2026-04-26c]

### Added
- `proposals/005-p09-public-release.md` — proposal file for this session's work (P-09 reword + sanitisation sweep + initial public release). Required by `proposals/README.md` convention: every PROPOSE survives `/clear` and session boundaries via its file.

### Changed
- `proposals/README.md` — row 005 added to index.
- `PROGRESS.md` — P-09 reword + initial public release logged as completed; GitHub repo link added.

### Process
- [Proposal 005](proposals/005-p09-public-release.md) DONE — `/code-reviewer` APPROVE; all DoD items green except T3 (pending 2026-05-16 per date-lock).

## [2026-04-26b]

### Changed
- `docs/RISK_FRAMEWORK.md` §Evaluator Failure Protocol — cold-reader reword (P-09a): replaced "the failure mode the deterministic gate-evaluation pipeline was built to retire" with explicit description of the prohibited pattern (agents substituting estimated/recalled values for unavailable gate data). [Proposal 003](proposals/003-phase-1b-data-integration.md) citation retained as structural breadcrumb.
- `PROGRESS.md` Completed bullet (2026-04 session) — cold-reader reword (P-09b): dropped temporal anchor "First session under the current War Room methodology"; retained descriptive parenthetical (Counter-Regime agent, 5-distinct-framework Strike Team) as the lead.
- `PROGRESS.md` §Open Process-Bias Items — P-09 ticked RESOLVED 2026-04-26.
- `SHARING.md` — added non-advice disclaimer blockquote at top of file; removed §"Cross-model critique (Delta Annexe)" section (user-global governance convention, not framework content).
- `docs/COMPLIANCE.md` — prepended jurisdiction disclaimer and non-advice header (file ships with Portugal as worked example; onboarding rewrites for new user's jurisdiction).
- `brainstorms/INDEX.md` — converted from personal session index to framework convention stub (personal index moved to `local/brainstorms/INDEX.md`, gitignored under existing `local/*` rule).

### Removed
- `docs/retros/` — stray directory containing a single duplicate retro artefact (`2026-04-26.md`). Canonical copies live in `local/retros/` per `skill-config.yaml`. Deleted to avoid tracking personal session data in the public framework.
- `docs/BACKTEST_REPORT.html`, `docs/PROJECT_OVERVIEW.html` — generated report artefacts moved to `local/` (gitignored). Generated outputs do not belong in the public framework manifest; the source data and generators (`scripts/generate_dashboard.py`, `backtesting/`) ship and recipients regenerate locally.

### Added
- `local/.gitkeep` — empty marker file; ensures `local/` directory is preserved on fresh clone for onboarding (`init_workspace.py` requires the directory to exist).
- `local/brainstorms/INDEX.md` — personal session index (moved from `brainstorms/INDEX.md`; gitignored).
- `.gitignore` — expanded database ignore rule from `data/*.db` to `**/*.db` and `**/*.sqlite`; covers generated SQLite databases in any subdirectory (e.g. `backtesting/engine/backtest.db`).

### Process
- Initial `git init` (this entry) — first public commit of the Quant Strategy Desk framework. Pre-commit sanitisation sweep: secrets scan (`git grep --cached`), PII grep, symlink audit, canary test, staged-file verification.

## [2026-04-26]

### Added
- `CHANGELOG.md` (this file) at project root — canonical, append-only framework-change record. Backfilled from `PROGRESS.md` §Session Log + §Architecture Decisions Log + the three closed proposals (001/002/003). See [Proposal 004](proposals/004-sanitisation-sweep-changelog.md).
- `Sanitisation Lint` anchor in `.claude/review-patterns.md` — semantic instruction (not a literal regex) that flags framework-internal version markers in user-facing markdown while ignoring external dependency / model / library version strings; converts proposal anchors to markdown citations.
- `proposals/_TEMPLATE.md` Status Log preamble — the closing entry MUST be paired with a `CHANGELOG.md` line (entry-or-it-didn't-happen).
- `.claude/commands/war-room/SKILL.md` §3b — explicit Session Close step: "if any file outside `local/` changed in this session, append a `CHANGELOG.md` entry."

### Changed
- `PROGRESS.md` §Session Log section deleted; replaced with a one-line pointer to `CHANGELOG.md`. PROGRESS.md is now forward-looking only.
- `PROGRESS.md` §Architecture Decisions Log section deleted; replaced with a one-line pointer to `CHANGELOG.md`. ADL rows migrated 1:1 into `### Decisions` subsections per dated CHANGELOG entry, with persona attribution preserved (`- (X, Persona Name) <decision> — <rationale>`).
- Sanitisation sweep across the public-facing framework manifest: framework-internal version markers (`War Room v2.0`, `Session #N`, `Phase 1A`, `Phase 1B`) stripped from user-facing prose; proposal anchors (`Proposal 001`, `Proposal 003`) converted to markdown citations linking to the proposal file. Files touched: `PROGRESS.md`, `proposals/README.md`, `.claude/commands/war-room/SKILL.md`, `docs/RISK_FRAMEWORK.md`, `docs/DATA_STANDARDS.md`, `brainstorms/_TEMPLATE.md`. Code provenance comments in `agents/macro_strategist.yml` and `agents/risk_guardian.yml` retained (debugger-facing, not cold-reader-facing).
- `proposals/README.md` Convention section: added the CHANGELOG-append rule on proposal close. Index row 003 title sanitised; row 004 added.

### Process
- [Proposal 004](proposals/004-sanitisation-sweep-changelog.md) DONE — pre-grep candidate scan executed; per-line three-way decision rule (STRIP / CONVERT / KEEP) applied across 8 in-scope files with marker hits and 9 zero-hit candidates recorded as "scanned, no edits needed"; DoD #4 zero-unjustified-matches assertion green; DoD #11 synthetic lint test green (Sanitisation Lint anchor fired on a manufactured `Phase 2A` insertion in `AGENTS.md`); DoD #10 cold-reader sub-agent smoke executed with verbatim findings recorded (one residual cold-reader fragility flagged for PROGRESS.md Open Process-Bias Items, not back-pressuring close per B1).
- (A, Quant Architect / R-A1, Gemini Round 2) Architecture Decisions Log relocated from `PROGRESS.md` into `CHANGELOG.md` `### Decisions` subsections — single canonical historical event log, persona attribution preserved.

### Decisions
- (B, Portfolio Manager / B2, Round 1) Pre-grep drives the actual edit set — proposal manifest is a candidate list; files with zero hits are recorded as scanned without gratuitous touch.
- (D, Risk Officer / D1, Round 1) Fourth-source tripwire — if a fourth source of framework history is discovered mid-execution (not Session Log, not Status Logs, not CHANGELOG, not ADL), execution pauses and the proposal is amended before resuming.

 — snapshot → GateReport evaluator with `Market_Risk_Tier` / `Data_Confidence_Tier` separation; sentinel-file walk-up for `PROJECT_ROOT`; float-boundary epsilon guard; canonical `compute_gates_content_sha()` public function (semantic-content lock recipe). See [Proposal 003](proposals/003-phase-1b-data-integration.md).
- `scripts/data/prompts.py` — `format_macro_prompt()` / `format_risk_prompt()` single-source prompt constructors.
- `scripts/data/parity_check.py` — mechanical parity check vs prose Phase 2 table; split-based parsing replacing fragile regex; header validation for column-order drift.
- `scripts/data/cli.py` + `__main__.py` — `gate_eval` subcommand.
- `tests/test_gate_eval.py` — 39 → 46 tests; `TestGatesContentSha` (recipe pinned, comment-insensitivity, data-change sensitivity); `TestArtefactSync` (REPLAY_DELTA + proposal Status Log must contain live canonical SHA).
- `tests/test_skill_invariants.py` — rewritten with dynamic agent discovery via `gate_consumption: true|false` field; `test_all_agents_declare_gate_consumption`; `_build_watched_files()` regex-based.
- `agents/macro_strategist.yml`, `agents/risk_guardian.yml` — `gate_consumption_rules`, `escalation_rules`, `output_format` added.
- `docs/RISK_FRAMEWORK.md` §Evaluator Failure Protocol.
- `backtesting/REPLAY_DELTA.md` — 12 BT sessions classified (0 BUG); canonical SHA in header.
- `backtesting/POST_MORTEM_BRIEF.md` §Gate Replay Findings.
- `local/snapshots/2026-04.json` + `local/snapshots/backtest/BT-01..BT-12.json` — 13 synthetic fixtures.

### Changed
- All 17 `agents/*.yml` carry a `gate_consumption: true|false` field; `macro_strategist` + `risk_guardian` = `true`.
- `agents/macro_strategist.yml`, `agents/risk_guardian.yml` consume the gate evaluator output; recall paths removed.
- `.claude/commands/war-room/SKILL.md` Step B rewritten with B.1–B.5 gate_eval procedure.
- `brainstorms/_TEMPLATE.md` Phase 2 gate table → `[GATE_EVAL_OUTPUT]` placeholder; Phase 7 `Tier_Override` Log added.
- `config/gates.yml` — schema/semantics comment block; semantic-content SHA lock recipe pinned.
- FRED warning routed to stderr.

### Process
- [Proposal 003](proposals/003-phase-1b-data-integration.md) DONE — 82 tests green / 1 skipped (live smoke); gate_eval CLI live; parity check 0 tier flips vs 2026-04 prose; 12 BT sessions replayed (0 unexplained divergences); rollback gate retracted pending live smoke 2026-05-16.

## [2026-04-25]

### Added
- `local/` segregation: 10 personal artefacts moved; `local/*` gitignore rule; templates created. See [Proposal 002](proposals/002-project-portability.md).
- `scripts/init_workspace.py` — idempotent, never ingests secrets; FRED detection verified.
- `SHARING.md` — fork/share guide for the framework.
- `skill-config.yaml` — root-level skill configuration anchor.

### Changed
- All framework files (`CLAUDE.md`, `AGENTS.md`, `agents/*.yml`, `docs/`, `_TEMPLATE.md`, `scripts/`) updated to `local/` paths.
- `PROGRESS.md` personal content extracted to `local/SESSIONS.md` (per-entry triage).
- Onboarding questionnaire expanded 5 → 11 questions.
- Session Staleness Check added for irregular contributors.

### Process
- [Proposal 002](proposals/002-project-portability.md) DONE — all 11 artefact moves confirmed; init_workspace.py written and tested; grep-clean standard applied across 44 files; Proposal 001 audit clean.
- Dual-model cross-check complete (Gemini Round 2); Delta Annexe captured in [Proposal 002](proposals/002-project-portability.md).

## [2026-04-23]

### Added
- Tier 1 macro data layer: `DataProvider` ABC, `SnapshotWriter` (canonical JSON + SHA256), `HttpClient` (backoff + on-disk cache), FRED provider, ECB provider, CLI (`python -m scripts.data fetch`). See [Proposal 001](proposals/001-data-layer-upgrade.md).
- `config/gates.yml` as single source of gate thresholds; prose de-duplicated to reference it.
- `docs/RISK_FRAMEWORK.md` §Data Degradation Protocol.

### Process
- [Proposal 001](proposals/001-data-layer-upgrade.md) DONE — all 7 DoD gates green; 54 offline tests pass; live smoke test passed (7 series: FRED×4 + ECB×3); snapshot written and hash verified; adversarial review of 14 Gemini loopholes (L1–L14) absorbed; Phase 1B commitment recorded.

## [2026-04-19]

### Process
- Process reform after the 2026-04 War Room (post-DFNS.PA false-negative investigation): Phase 1 restricted to macro state — no candidate ticker fetches; Phase 2.5 Candidate Instrument Verification added post-thesis with mandatory Data Failure Protocol (retry → alt format → alt exchange → alt source); Phase 1.5 Counter-Regime Analysis (mandatory parallel Sonnet agent); carry-forward brief scoped to Orchestrator only, stripped from Strike Team prompts; Phase 7 context brief gutted (agents now judge cold); pre-session live log (`local/brainstorms/YYYY-MM.pre-session.md`) written before Strike Team runs for tamper resistance. Files touched: `SKILL.md`, `_TEMPLATE.md`, `CLAUDE.md`, `AGENTS.md`. Protocol Audit checklist expanded 9 → 12 checks.

### Decisions
- (A, Quant Architect) Session Go/No-Go Check — new mandatory section in `brainstorms/_TEMPLATE.md`, placed between Staleness Check and Strike Team Selection. Three verdicts: GO / DEFER EXECUTION / POSTPONE. Covers binary risk events (Hormuz, escalation), imminent data releases, VIX Emergency Protocol, and broker accessibility. Distinct from deployment gates (Phase 1) — gates govern capital deployment; Go/No-Go governs whether to run the session at all. Added to CLAUDE.md mandatory sections checklist.

## [2026-04-06]

### Decisions
- (D, Risk Officer) ETF-only universe (no individual stocks) — monthly cadence too slow for single-stock event risk (earnings gaps, scandals). Micro-NAV makes single stocks = lottery tickets. Thematic ETFs capture sector tailwinds with diversification. Survivorship bias in stock-picking ("the next Novo Nordisk") ignores the many failures. Review at S-5 trigger.
- (A, Quant Architect) Structured Strike Team output formats — standardises agent outputs (tables, not prose) for cross-session comparability without constraining analysis. Independence rule: agents must disagree with orchestrator framing when their data says otherwise.
- (C, CTO) Tiered read order — Always: AGENTS.md, PROGRESS.md, local/INVESTOR_PROFILE.md, local/PORTFOLIO.md, RISK_FRAMEWORK.md, STRATEGY_LOGIC.md. On-demand: agent YAMLs (at launch), DATA_STANDARDS.md, COMPLIANCE.md. Saves ~10k context tokens per session.
- (B, Portfolio Manager) Hypothesis Log for between-session ideas — natural-language scratchpad for investment observations. Orchestrator formalises at session start. Signal Generator investigates alongside own signals. Precursor to edge pipeline (S-7).
- (A, Quant Architect) Session + Trade Scorecards — Session Scorecard (5 metrics in Handoff) and Trade Performance Review (closed positions + running stats) added to template. Precursor to Karpathy self-improvement (S-2).

## [2026-04-05]

### Decisions
- (D, Risk Officer) Tiered cash floor (30% → 15% Standard) — 30% floor was single biggest performance drag (66.5% deployment). Tiered by NAV: micro €200, Standard 15%, Full 10%.
- (B, Portfolio Manager) AMBER deployment gates — binary GREEN/RED replaced with GREEN/AMBER/RED. AMBER = half-rate deployment. 8 gates (added EUR/USD, reclassified tariff).
- (A, Quant Architect) Thesis Validation Protocol — valid thesis types defined. Invalidated theses must be re-proposed, exited, or grandfathered with sunset. Prevents drift.
- (D, Risk Officer) VIX Emergency Protocol — VIX >35 = zero deployment + mandatory review. Fills gap between AMBER gates and kill switch.
- (A, Quant Architect) Session Close Checklist (replaces /commemorate) — project is not a git repo. /commemorate never ran. Process Sheriff checks embedded in session template.
- (B, Portfolio Manager) Asset-class targets (generic, not ticker-specific) — prevents backtest bias in live portfolio. War Room decides instruments; framework sets asset-class ranges.

## [2026-04]

### Process
- Portability & Critical Review session — in-place genericisation of framework files for shareability; onboarding questionnaire expanded 5 → 11 questions; Session Staleness Check added for irregular contributors; critical review delivered 7 improvements implemented and 2 proposals dropped (MCP, pre-session script); Staged Improvements list created (S-1 to S-10) with trigger conditions.

## [2026-03-28]

### Decisions
- (D, Risk Officer) Risk Guardian fixed (Two Sigma) — risk methodology needs month-to-month consistency; Challenger slot provides secondary risk perspective. Upgrade to paired Risk at NAV > 5,000.
- (B, Portfolio Manager) Macro Strategist now rotating — different lenses (Bridgewater regime, AQR factor, Man Group trend) catch different things. Was incorrectly fixed with no alternatives.
- (A, Quant Architect) 5-agent Strike Team standard — added Challenger role. Execution/Compliance were never included despite own retrospective saying to. 8 of 15 agents had never served on Strike Team.
- (B, Portfolio Manager) Session scheduling: third Saturday — after NFP, CPI, ECB. Weekend for reading. 10+ days before month-end for execution.
- (A, Quant Architect) War Room process refresh — `/war-room` skill (prep+session+sheriff), rotation enforcement, agent performance tracking, price affordability filter, carry-forward briefs.

## [2026-03]

### Process
- Foundation session — created governance framework (6-persona model, 8-step methodology); generated 15 specialist agent persona YAML configurations; established documentation structure (`docs/`, `agents/`, `brainstorms/`); created portfolio ledger and War Room template with 7 phases + 15-agent sign-off.

## [2026-03-01]

### Decisions
- (B, Portfolio Manager) European equities as primary universe — user is Europe-based; accessibility on standard platforms.
- (A, Quant Architect) 6-persona governance model (A–F) — covers architecture, PM, CTO, risk, execution, and compliance perspectives.
- (C, CTO) Python as primary language — industry standard for quant finance; rich ecosystem (pandas, numpy, scipy).
- (B, Portfolio Manager) Monthly cadence, no day-trading — investor places trades once/month at War Room; no intraday monitoring.
- (B, Portfolio Manager) European preference, not restriction — global instruments allowed if clearly superior and accessible on EU brokers.
- (D, Risk Officer) Defined risk only (no naked shorts) — investor must never owe more than capital invested.
- (D, Risk Officer) Instrument-specific stop-losses — -3% floor + vol stops for ETFs; flat -3% fires on ETF noise at VIX 18+.
- (A, Quant Architect) Role-based agent selection — `Task()` examples use role placeholders, not hardcoded agent names, to prevent bias.
- (B, Portfolio Manager) Staged deployment (3-month) — first allocation 15% → 45% → 65% over March–May; binary risks require cash optionality.
