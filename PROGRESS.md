# Quant Strategy Desk — Progress & Roadmap

> Last updated: 2026-06-10

## Current State

### ✅ Completed
- **Governance framework**: `AGENTS.md`, `CLAUDE.md`, `PROGRESS.md`, `local/PORTFOLIO.md` established
- **Agent personas**: 15 specialist YAML configs in `agents/` — all updated with shared investor profile
- **Documentation**: Strategy logic, risk framework, data standards, compliance (Portugal-specific) docs created
- **War Room template**: `brainstorms/_TEMPLATE.md` with 7 phases, anti-bias rule, anti-assumption rule, 15-agent sign-off
- **First War Room session (2026-03)**: Completed. Trade outcomes, regime call, and P&L in `local/SESSIONS.md`.
- **Historical simulation**: 12-month backtest complete (March 2025 → February 2026). See `backtesting/` for methodology, sessions, and results.
- **12-month backtest post-mortem**: Completed. Analysed all 20 topics (12 user questions + 8 orchestrator observations). Strike Team challenged the improvement plan (Two Sigma Risk, Bridgewater Macro, Renaissance Challenger). Output: 18 implemented changes across RISK_FRAMEWORK.md, AGENTS.md, _TEMPLATE.md, CLAUDE.md, STRATEGY_LOGIC.md. Key changes: tiered cash floor (30% → 15% at Standard NAV), AMBER deployment gates, Thesis Validation Protocol, VIX Emergency Protocol, Session Close Checklist (replaces /commemorate), regime accuracy retrospective.
- **Portability refactoring**: In-place genericisation of hardcoded references across framework files. Onboarding expanded to 11 questions (contribution style, cash reserve, ETF-only disclosure). Session Staleness Check added for irregular contributors.
- **Process reform (2026-04-19, post-2026-04 session)**: Fixed availability bias + 4 related loopholes in War Room framework. Triggered by DFNS.PA false-negative investigation (yfinance transient failure mis-read as structural unavailability). Changes: (1) Phase 1 restricted to macro state — no candidate ticker fetches; (2) Phase 2.5 Candidate Instrument Verification added post-thesis with mandatory Data Failure Protocol (retry → alt format → alt exchange → alt source); (3) Phase 1.5 Counter-Regime Analysis (MANDATORY parallel Sonnet agent); (4) Carry-forward brief scoped to Orchestrator only, stripped from Strike Team prompts; (5) Phase 7 context brief gutted — agents now judge cold; (6) Pre-session live log (`local/brainstorms/YYYY-MM.pre-session.md`) written before Strike Team runs for tamper resistance. Files touched: `SKILL.md`, `_TEMPLATE.md`, `CLAUDE.md`, `AGENTS.md`. Protocol Audit checklist expanded 9 → 12 checks.
- **Critical review + improvements**: Implemented 7 improvements: structured agent output templates, trade/session scorecards, tiered read order (~10k tokens saved), Hypothesis Log, enriched agent roster, Monthly Risk Dashboard fix, Staged Improvements list (S-1 to S-10). Dropped MCP data integration (not worth it at current scale) and pre-session script (portability issues).

- **Second War Room session (2026-04)**: Completed. Ran with Counter-Regime agent and 5-distinct-framework Strike Team. Trade outcomes, regime call, and P&L in `local/SESSIONS.md`.
- **Data Layer Upgrade — Tier 1 macro substrate**: COMPLETE (2026-04-23). FRED + ECB providers, canonical JSON snapshots with SHA256, `config/gates.yml` as single source of gate thresholds, Data Degradation Protocol in `docs/RISK_FRAMEWORK.md`, 54 tests green, live smoke passed. See [Proposal 001](proposals/001-data-layer-upgrade.md).
- **Sanitisation Sweep & CHANGELOG Introduction**: COMPLETE (2026-04-26). Stripped framework-internal version markers from public-facing prose; converted proposal anchors to markdown citations; introduced root-level [`CHANGELOG.md`](CHANGELOG.md) as canonical append-only framework-change record (backfilled 2026-03-01 → 2026-04-26 from PROGRESS.md §Session Log + §Architecture Decisions Log + closed proposals); migrated both Session Log and Architecture Decisions Log out of PROGRESS.md (replaced with pointers); added Sanitisation Lint anchor to `.claude/review-patterns.md` and CHANGELOG-append step to SKILL.md Session Close. See [Proposal 004](proposals/004-sanitisation-sweep-changelog.md).
- **Data Layer Integration**: COMPLETE (2026-04-26). gate_eval wired into War Room workflow; 82 tests green (7 added in post-review hardening); SKILL.md + agent YAMLs rewired; parity check passed (0 tier flips vs 2026-04 prose); 12 BT sessions replayed (0 unexplained divergences); Evaluator Failure Protocol in RISK_FRAMEWORK.md; rollback gate retracted pending 2026-05-16 live smoke. Post-review hardening: `compute_gates_content_sha()` public canonical recipe; artefact-sync tests; dynamic agent invariant test with `gate_consumption: true|false` field on all 17 agents; FRED warning routed to stderr; provenance receipt in pre-session log header; parity_check table parsing rewritten. See [Proposal 003](proposals/003-phase-1b-data-integration.md).
- **Windows Git prerequisite added to README**: COMPLETE (2026-04-27). Requirements section and Quick Start callout updated so Windows users know to install Git for Windows before opening Claude Code. See [Proposal 006](proposals/006-windows-git-prereq-readme.md).
- **First-run pre-flight guard for `/war-room`**: COMPLETE (2026-04-27). Skill now halts before Phase 0 when `local/INVESTOR_PROFILE.md` is missing or empty, and routes to the 11-question onboarding flow instead of silently stalling. Three smoke tests passed (profile present, profile absent, zero-byte file). See [Proposal 007](proposals/007-war-room-first-run-guard.md).
- **Phantom skills cleanup**: COMPLETE (2026-04-27). Deleted three phantom Local skill rows (`/audit`, `/backtest`, `/risk-check`) from CLAUDE.md Skills table; build-out deferred as Staged Improvement S-13. See [Proposal 008](proposals/008-phantom-skills-cleanup.md).
- **Hypothesis-log lifecycle (War Room)**: COMPLETE (2026-06-02). `/war-room` SKILL.md now names `local/HYPOTHESIS_LOG.md` at Phase 4; `brainstorms/_TEMPLATE.md` gained a plain anti-bias line (user hypotheses investigated *alongside*, never in place of, the Signal Generator's own search; reported with a Status + Finding; the orchestrator records the verdict and may dismiss a thin "confirm") and a stamp-in-place close-out (append `<YYYY-MM>:<STATUS>` to each open idea's new `Last Reviewed` column — never move or delete). The `## Investigated` archive section was retired in both the tracked template and the live log; pre-existing `local/local/` path typos in `_TEMPLATE.md` fixed. Supersedes [Proposal 020](proposals/020-warroom-skill-hypothesis-log-read.md) (pointer-only), bundled per operator request; converged across four isolated-Challenger rounds by scope-reduction (stamp-in-place replaced a lossy move-to-archive that had generated a structural defect cluster). See [Proposal 021](proposals/021-hypothesis-log-bias-guard-and-closeout.md).
- **P-09 reword + initial public release**: COMPLETE (2026-04-26). Cold-reader rewording of two P-09 sites (RISK_FRAMEWORK.md §Evaluator Failure Protocol, PROGRESS.md 2026-04 session bullet). Full sanitisation sweep; `brainstorms/INDEX.md` converted to stub; `docs/retros/` deleted; generated HTML artefacts moved to `local/`; `.gitignore` expanded to `**/*.db` and `**/*.sqlite`; non-advice disclaimer added to SHARING.md and COMPLIANCE.md; cross-model critique section removed from SHARING.md (user-global, not framework). Initial commit `fd4d036` pushed to [github.com/dvscampos/quant-strategy-desk](https://github.com/dvscampos/quant-strategy-desk). See [Proposal 005](proposals/005-p09-public-release.md).

### 🔄 In Progress

- **Pre-push sanitisation remediation** ([Proposal 029](proposals/029-pre-push-sanitisation-remediation.md)) — **reversible build + item-4 tree-triage committed 2026-06-11; PHASE-5 squash + local archive complete 2026-06-12; public push (PHASE-7) pending (separate gate).** `main` is ahead of the PUBLIC `origin/main`; a pre-push scan found range-diff collisions between private-ledger values and tracked content. Committed so far: (1) a pre-PUSH sanitisation hook (`main_prepush` + `range_added_text`, new `scripts/hooks/pre-push`, hook-type-aware `install_hook`) closing the root-cause gap; the reconcile fixtures rebuilt fully-synthetic + a DoD-6 entry-point test; `proposals/025` scrubbed. (2) **Item-4 tree-level triage (source-driven, after an operator steering correction supersededed the unsafe sig-fig basis):** the 20 net-tree collisions resolved by HARVEST SOURCE — 3 SCRUB (real holding ISIN + two real economics figures that sat in tracked proposal PROSE: an IBKR commission and a partnership-contribution total), 4 CLEAN-THE-LOG (meta-only steering-log self-poisoning, no allowlist blind-spot), 13 ALLOWLIST (public tickers + public-macro/coincidental numerics). **Net-tree `find_leaks==0` verified by bytes; suite 122/1; dual-use blind-spots 15.** 122 passed / 1 skipped throughout. **REMAINING (separate irreversible gate):** the public push (PHASE-7). **PHASE-5 squash + local archive COMPLETE (2026-06-12):** the unpushed commits consolidated into one clean commit; the full pre-rewrite history retained on a local archive ref (never pushed); the pre-push sanitisation hook installed-AND-OURS and runtime-verified; the per-commit/push gate is now reachable (single-commit net-tree `find_leaks==0`). **Net-tree==0 was the item-4 MILESTONE and is NECESSARY-NOT-SUFFICIENT for the push — the per-commit/push gate clears only post-squash, now met; the push itself remains a separate explicit operator gate.** Connects to S-30/S-31 (banked: S-31 should exclude `local/brainstorms/*steering*` + operational scratch from the harvest corpus — the self-poisoning root cause).

### 🔜 Next Up

**Next War Room session — #4, scheduled 2026-06-20** (third Saturday of June; lands after the imminent ECB decision). The 2026-05 session (#3) is COMPLETE — first 0-trade HOLD session under the most defensive gate configuration to date. Surfaced five data-layer issues now tracked as S-22–S-25 below.
> **DoD #10b**: ✅ CLOSED 2026-05-15. `python3 -m scripts.data fetch --session 2026-05 && python3 -m scripts.data gate_eval --session 2026-05` ran clean — 7 live observations (4 FRED + 3 ECB), snapshot `sha256:83207ac933d7…`, gate_eval rendered. Data-layer-integration rollback clock (2026-06-20) **retracted**. The RED Data_Confidence_Tier is expected — it reflects manual gates (hormuz/brent/tariff/stoxx600) not yet wired into the FRED/ECB pipeline, not a smoke failure.

**Known issues to address**
- `yfinance` Series format error (`TypeError: unsupported format string passed to Series.__format__`) — ✅ CLOSED 2026-05-15 after fresh audit: the engine path in `backtesting/engine/data.py:152-159` has carried the `hasattr(val, "iloc")` → `float(val.iloc[0])` fallback since the initial commit `fd4d036`. The BT #12 occurrence originated from agent-improvised yfinance code dispatched via `Task()` sub-agents, not engine code — addressed structurally by the engine-first workflow rule (MEMORY: `feedback_engine_first_workflow.md`). No code fix needed.

### 🔍 Open Process-Bias Items (from 2026-04-19 Opus audit)

> Tier 1 items (availability bias, carry-forward anchoring, regime-label single anchor, Phase 7 theatre, retrospective-narrative bias) resolved in the 2026-04-19 process reform. Items below are Tier 2/3, logged for later treatment. **Do not let them drift.**

- [ ] **P-01 Orchestrator circularity (Tier 2).** The Opus Orchestrator writes Strike Team prompts AND synthesises their outputs — same entity on both ends. Any Orchestrator framing bias is invisible to the loop. Candidate fix: a second Opus pass acting as "devil's orchestrator" that reads the same raw outputs and produces an independent synthesis, compared before the trade plan is locked.
- [x] **P-02 Small-sample rule overfitting (Tier 2). RESOLVED 2026-04-20.** Session Scorecard now includes a "New Rules Adopted This Session" section; every rule must be tagged `(origin: session #N, evidence: X observations)`. Rules require N≥10 observations before promotion to canon.
- [ ] **P-03 Gate threshold cliff edges (Tier 2).** Brent $99 vs $101 behaves discontinuously despite equivalent signal. Thresholds declared not derived. Candidate fix: graduated gate scoring (0–1 continuous) rather than GREEN/AMBER/RED; or calibrate thresholds against historical regime-change frequency.
- [x] **P-04 Rotation diversifies personas, not viewpoints (Tier 3). RESOLVED 2026-04-20.** `analytical_framework` field added to all 15 agent YAMLs (macro-narrative / quantitative / fundamental / behavioural / flow-based). SKILL.md rotation check and _TEMPLATE.md Rotation table now enforce ≥2 distinct frameworks per Strike Team.
- [x] **P-05 Thesis taxonomy as implicit filter (Tier 3). RESOLVED 2026-04-20.** Added "Crowded trade continuation" and "Narrative-driven flow" as first-class thesis types in `AGENTS.md`. Added escape-hatch option: agent may write "Does not fit taxonomy — [plain-language thesis + invalidator]" to prevent taxonomy pressure distorting honest thesis formation.
- [x] **P-06 Self-congratulating Session Scorecard (Tier 3). RESOLVED 2026-04-20.** Scorecard split into (a) Process Compliance (gates, regime confidence, deployment efficiency, thesis diversity, checklist score, framework diversity) and (b) Ex-Post Outcome Metrics filled at next session (regime-call accuracy T+30, trade IRR vs benchmark T+90, Phase 7 FLAG resolution rate). _TEMPLATE.md updated.
- [ ] **P-07 Streetlight-effect deployment gates (Tier 3).** Gates drift toward what yfinance exposes (VIX, Brent, ECB) rather than what predicts (credit spreads, PMI surprise, funding stress, positioning). Candidate fix: define target gate set independent of data availability; track data-sourcing gap as a separate backlog.
- [ ] **P-08 Implicit cost blindness (Tier 3).** Cost model includes FTT/stamp duty but ignores bid-ask on illiquid UCITS, ETF tracking error, FX spreads, internal fund cash drag. Matters more at micro-NAV. Candidate fix: extend transaction cost model in `docs/STRATEGY_LOGIC.md` with estimated ranges for each.
- [x] **P-09 Cold-reader fragility in proposal-citation prose (Tier 3). RESOLVED 2026-04-26.** Surfaced by the 2026-04-26 cold-reader smoke (Proposal 004 DoD #10). Two prose sites read fragile to a contributor with no project history: (a) `docs/RISK_FRAMEWORK.md` §Evaluator Failure Protocol — "the failure mode the deterministic gate-evaluation pipeline was built to retire" assumes a known predecessor; the cited [Proposal 003](proposals/003-phase-1b-data-integration.md) carries the *why* but the rule should also stand more cleanly on its own; (b) `PROGRESS.md` Completed bullet — "first session under the current War Room methodology" is a temporal anchor that implies a prior methodology without naming the change. Candidate fix: rephrase both to either name the predecessor explicitly or drop the temporal contrast. Logged for follow-up; does not back-pressure Proposal 004's close (B1).

### 📋 Backlog
- [ ] **Exit alert system** — automated notifications for stop-loss, kill switch, dividend cut, and macro triggers (email/SMS/push). Interim: set broker-level GTC stop-losses at trade entry.
- [ ] **CVaR/ES risk module** — conditional value-at-risk for tail risk measurement
- [ ] **Portfolio optimisation module** — Black-Litterman or risk parity for when portfolio exceeds 40% invested
- [ ] **MCP Elicitation checkpoints** — structured pause points in War Room for deployment gates, stop-loss confirmation, and position verification. Requires MCP server setup + Claude Pro support. Review feasibility each session.
- [ ] Data pipeline implementation (market data ingestion)
- [ ] Signal generation module (first alpha signals)
- [ ] Backtesting engine (event-driven with bias checks)
- [ ] Risk management system (position sizing, stop-losses, VaR)
- [ ] Paper trading integration (IBKR)
- [ ] Live execution system architecture
- [ ] Compliance monitoring automation

### 🔮 Staged Improvements (activate when trigger condition is met)

> These are improvements that aren't worth deploying yet but should be activated once the project reaches the right scale or data maturity. Check triggers at each War Room session.

| # | Improvement | Trigger Condition | Why Not Now | Compatible With Current? |
|---|---|---|---|---|
| S-1 | **Numeric scoring framework (0-100)** — weighted composite scores for gates, regime confidence, position health | 6+ live sessions with complete Session Scorecards | Need data to calibrate what scores actually predict. Categorical (GREEN/AMBER/RED) captures the signal for now. | Yes — current categorical scorecards upgrade directly to numeric |
| S-2 | **Karpathy self-improvement loop** — systematic bias detection, agent quality ranking, automated parameter adjustment | 12+ live sessions with complete scorecards AND trade performance data | Requires enough data to detect statistical patterns. Premature with <12 data points. | Yes — scorecards + trade performance are the data collection layer |
| S-3 | **Automated trade performance analysis** — IBKR CSV import, FIFO matching, statistical analysis per thesis type, behavioural pattern detection | 20+ closed trades with complete thesis metadata | Need enough closed trades for statistical significance. Manual tracking works until then. | Yes — manual Trade Scorecard collects the same fields; CSV automation replaces manual entry |
| S-4 | **Backtesting engine (code)** — Python event-driven engine for testing codifiable strategy hypotheses (signal validation, walk-forward, anti-overfitting) | When you have a specific, codifiable strategy hypothesis to test (not discretionary macro allocation) | Current approach is discretionary macro — no codifiable rules to test. BT1-12 tested the *process*, not a *strategy*. | Yes — STRATEGY_LOGIC.md signal validation pipeline is the specification |
| S-5 | **Individual stocks sleeve** — 5-10% NAV allocation for high-conviction single-name positions | NAV > €10k AND weekly (not monthly) review cadence AND fundamental analysis capability added | At current NAV, single stocks = lottery tickets. Monthly cadence is too slow for single-stock risk. ETFs capture thematic tailwinds with diversification. | N/A — would require new agents and data sources |
| S-6 | **Phase 7 reduction** — reduce sign-off from 10 to 6-8 most signal-producing agents | 6+ live sessions with `local/AGENT_PERFORMANCE.md` data showing clear noise vs signal separation | Need performance data to know which agents add value. Haiku cost is negligible, so premature optimisation. | Yes — just remove agents from the roster call |
| S-7 | **Heavyweight edge pipeline** — multi-stage hypothesis → backtest → review → signal aggregation → postmortem with structured skill handoffs | Backtesting engine exists (S-4) AND 12+ sessions of hypothesis log data | Requires codified strategies and automated backtesting. Current hypothesis log + manual War Room review handles the workflow for now. | Yes — Hypothesis Log is stage 1 of the pipeline |
| S-8 | **Platform adaptation guide** — documentation for running the framework on Gemini CLI, OpenAI Codex, Cursor | When sharing the project with someone who uses a non-Claude platform | No one has asked yet. Multi-agent features degrade on other platforms — guide needed to explain what works and what doesn't. | N/A — documentation only |
| S-9 | **Pre-session automation script** — Python script for NAV calculation, gate checks, correlation matrix, stop-loss proximity | When session preparation consistently takes >15 minutes of in-session time | Currently handled by /war-room skill Phase 1. External script adds fragility (timing, portability) without enough payoff at current scale. | Yes — script would output the same data Phase 1 currently computes |
| S-10 | **Fundamental analysis agents** — new agent YAMLs for company-level analysis (earnings, balance sheets, competitive positioning, management quality) to support individual stock picking | When a user indicates interest in individual stocks during onboarding (see CLAUDE.md Q11 follow-up) | Current agent roster is designed for macro/factor/ETF analysis. Individual stocks require a different analytical lens. | Yes — new agents slot into the existing Strike Team rotation system |
| S-11 | **Session checkpoint system** ✅ — writes `local/brainstorms/.checkpoint-YYYY-MM.json` after Phase 1 and Phase 7; `/war-room` skill reads it on resume so mid-session context compaction loses nothing | **ACTIVE** — implemented 2026-04-19 | Was: no recovery path if context compacted mid-session. JSONL transcript existed but required manual digging. | Yes — additive to SKILL.md; no existing files changed |
| S-12 | **HTML dashboard** ✅ — `scripts/generate_dashboard.py` generates `local/dashboard.html` from `local/brainstorms/` session files + `local/PORTFOLIO.md` | **ACTIVE** — implemented 2026-04-19 | Was: data lived only in Markdown. No visual MoM tracking. | Yes — read-only parser; does not modify any framework files |
| S-13 | **Local skills: `/audit`, `/backtest`, `/risk-check`** — build the three phantom skills removed from CLAUDE.md Skills table by Proposal 008 (2026-04-27); `/audit` = data freshness + risk param validation + doc sync; `/backtest` = standardised BT with bias checks and tear sheet; `/risk-check` = Risk Officer portfolio-level assessment | When the framework has ≥6 live sessions of data AND the backtesting engine (S-4) is active | Deferred in P-008: building all three properly requires per-skill pre-flight, persona wiring, and integration with risk/data layers — HEAVY work not scoped for today. | Yes — skills slot into `.claude/commands/`; no framework files changed |
| S-14 | **Resolve redundant agent pairs flagged by Phase D `/persona-eval`** ✅ — Two within-role pairs scored 2/5 differentiation on 2026-05-15. **DONE 2026-05-15 via [Proposal 009](proposals/009-s14-redundant-agent-pairs.md)**: (a) `bridgewater_macro` renamed to `pimco_curve_strategist` with new `yield-curve-regime` framework and structurally distinct `duration_positioning_recommendation` output schema; (b) `de_shaw_statarb` rewritten as `structural` arbitrage specialist (share-class selection, cross-listed, tracking-difference). Median pair-differentiation: 2/5 → 4/5 on both pairs. Phase 7 panel slot #8 preserved. | Done | Phase D produced eval-grounded recommendations; Proposal 009 executed the rewrites with median-of-3 evals, dual cold reviewers, Phase 2 + investor-fit dry-runs, and staged merge. | Yes — agent YAMLs are independent files; rotation system already supports framework-distinct pools |
| S-15 | **Lift `citadel_alpha` persona quality** ✅ — **DONE 2026-05-15 via [Proposal 010](proposals/010-s15-s16-persona-quality-lift.md)**: rewrote `citadel_alpha.yml` with structured 7-field `output_format` schema (signal_proposal / IC / decay_curve / regime_dependence / capacity_estimate / long_only_etf_translation / compliance_uncertain), Citadel-specific "assume the signal is data-mined" adversarial review frame with 4 quantitative checks (multiple-testing correction, OOS decay, regime non-stationarity, capacity-vs-frequency mismatch), and jurisdiction-driven instrument compliance guard linked to INVESTOR_PROFILE.md. Vocabulary discipline guards Bridgewater/PIMCO/de_shaw/macro_strategist/aqr territory. Median-of-3 composite +13 (14→27); D3 3→4; cold-reviewer cross-check PASS (every dim ≥ original); pair-diff vs point72/dimensional/aqr 4/4/4 (after AQR forbidden-vocab fix during eval); investor-fit dry-run PASS. | Done | Eval baseline appended to `~/.claude/persona_eval_history.csv` for 2026-08-15 quarterly re-eval. | Yes — single-file rewrite, rotation system unaffected. |
| S-16 | **Lift D3 (Actionability) across the 17-agent persona library** ✅ — **DONE 2026-05-15 via [Proposal 010](proposals/010-s15-s16-persona-quality-lift.md)**; closing-example polish completed 2026-05-16 via [Proposal 012](proposals/012-citadel-twosigma-example-trio-polish.md) (citadel_alpha.yml and two_sigma_risk.yml — the two files still carrying the pre-amendment generic example trio — now ship archetype-native trios; verified by full enumeration grep across all 17 personas): appended uniform actionability template (HIGH/MEDIUM/LOW severity + specific fix + priority order, irreversible risks first) to all 17 personas' `review_prompt` blocks. Each persona ships **archetype-native fix examples** (macro → gate/regime triggers; risk → stops/VaR; execution → TWAP/venue; signal → IC/decay; compliance → KID/MiFID; etc.) so cross-domain bleed is structurally prevented. virtu_execution received a Real-time-Execution-Stop amendment to harmonise its pre-existing CRITICAL/HIGH/MEDIUM scale to the project HIGH/MEDIUM/LOW standard while preserving its effort-estimate language. Stratified-sample eval (5 personas): median-of-3 PASS 5/5 (D3 3→4 on 4 of 5; virtu lateral); cold-reviewer PASS 5/5; synthetic Phase 8 dry-run PASS 5/5 contract; bleed-close re-eval PASS 3/3 after option-b per-persona-examples amendment. | Done | All 17 review_prompts now carry the uniform contract. Marker count = 17. | Yes — review_prompt-only edits; no schema breaks; critical patterns preserved verbatim. |
| S-17 | **Lift `renaissance_backtesting` persona quality** ✅ — **DONE 2026-05-21 via [Proposal 017](proposals/017-s17-renaissance-backtesting-quality-lift.md)** (standalone successor to the SUPERSEDED [Proposal 016](proposals/016-s17-s18-persona-quality-lift.md) S-17+S-18 bundle — 016's virtu half was structurally unconverged across three isolated-challenger rounds, so the bundle was split): rewrote `renaissance_backtesting.yml` with a structured 7-block `output_format` schema (backtest_subject / rolling_sharpe_stability / out_of_sample / transaction_cost_realism / turnover_cost_ratio / regime_conditioned_hit_rate / overfitting_verdict), a calibration-window-overfit adversarial `review_prompt` with 6 numbered checks, `frictionless_flag`→HIGH-severity and `overfitting_verdict`/`stability_verdict` coherence mandatory rules in `system_prompt`, and vocabulary discipline against dimensional/citadel/pimco. Legacy free-prose "full Python code" output mandate removed; critical-pattern fields preserved verbatim. Byte-verbatim eval battery: median-of-3 composite 28→33 (5 of 7 dimensions +1; D3 Actionability and D6 Environmental Adaptation flat); zero-regression; pair-diff vs dimensional 3→4 and vs citadel 4→4; dual cold reviewer PASS; both-branch dry-run discriminates overfit→likely-overfit / sound→real-alpha. | Done | Eval row appended to `~/.claude/persona_eval_history.csv` (composite 33) for the 2026-08-15 quarterly re-eval. | Yes — single-file rewrite, rotation system unaffected. |
| S-18 | **Re-architect `virtu_execution` persona** ✅ — **DONE 2026-05-22 via [Proposal 018](proposals/018-s18-virtu-execution-rearchitecture.md)** (standalone successor to the SUPERSEDED [Proposal 016](proposals/016-s17-s18-persona-quality-lift.md) S-17+S-18 bundle): re-architected `virtu_execution.yml` from a fictional institutional execution-algorithm developer into a retail execution-quality analyst for the desk's actual investor. Free-prose `output_format` replaced with a structured 5-block decision-rule schema (order_context / order_type_rule / timing_rule / cost_efficiency / execution_quality_rationale); `system_prompt` re-scoped to the retail-DCA context with four behavioural constraints; `capabilities` rewritten to the retail surface; `review_prompt` reframed to four retail-execution checks; `name`/`role` changed; `firm`/`gate_consumption`/`analytical_framework` preserved byte-identical; pre-rewrite persona archived at `proposals/018-backups/`. Investor literals (contribution amount, broker) **not** hardcoded — read from `local/INVESTOR_PROFILE.md` at runtime (APPROVE-WITH-AMENDMENTS de-hardcode, per the repo's `local/`-segregation convention). Byte-verbatim eval battery (DoD #11–#19): median-of-3 composite — rewrite **31** (clears the absolute 30 threshold), measured against the original re-measured on the *same* retail scenarios at **25 gate-applied / 27 pre-gate**; the planted-bug cap on the original legitimately reflects an in-domain miss (no commission-consolidation lens in the institutional review checks). The 31 is **not** comparable to the 2026-05-15 baseline of 29 — different scenario basis (retail vs institutional). Zero-regression held; D1 and D6 both improved (diagnosis confirmed); dual cold reviewer both GENUINE IMPROVEMENT; pair-diff 3→4 vs jane_street_mm, 4→4 vs millennium_live_trading; both-branch dry-run discriminates. | Done | Eval row appended to `~/.claude/persona_eval_history.csv` (composite 31, dated 2026-05-21); that retail-scenario score is **not** directly comparable to the 2026-05-15 institutional baseline — see the Proposal 018 Status Log. | Yes — single-file rewrite, rotation system unaffected. |
| S-19 | **Carry-forward instrument-compliance guard to `pimco_curve_strategist` and `de_shaw_statarb`** ✅ — **DONE 2026-05-16 via [Proposal 014](proposals/014-s19-compliance-guard-pimco-deshaw.md)**: `pimco_curve_strategist.yml` got the citadel-pattern schema field (`compliance_uncertain: boolean` in `output_format`) plus a `bond_etf_allocation`-targeted "Instrument compliance constraint" paragraph in `system_prompt`. `de_shaw_statarb.yml` (free-prose `output_format`) got an equivalent system_prompt clause with an **unconditional memo-header mandate** (`compliance_uncertain: [true\|false]` on every memo, regardless of state, for grep-able Phase 8 enforcement — strengthened from the original draft after `/code-reviewer` Note 2). Static-label enumeration honoured: phrasing now in 3 signal-pool personas (citadel + pimco + de_shaw); no bleed to gs_compliance, risk_guardian, or any other peer. | Done | Schema-vs-memo divergence between pimco and de_shaw is deliberate — converting de_shaw's `output_format` to structured YAML is a S-17/S-18-class HEAVY rewrite, deferred. | Yes — additive guard paragraph + (pimco only) one schema field. No critical-pattern fields touched. |
| S-20 | **Correct two actively-contradictory `AGENTS.md` archetype lines + tidy a `gs_compliance` example literal** ✅ — **DONE 2026-05-22 via [Proposal 019](proposals/019-s20-agents-archetype-correction.md)**: `AGENTS.md` Execution archetype line re-worded from institutional vocabulary (`venue / routing / TWAP / child-order`, banned by [Proposal 018](proposals/018-s18-virtu-execution-rearchitecture.md)) to `virtu_execution`'s retail surface (`order-type / timing / commission-drag / fractional-share`); `renaissance` split off the "Quant signal" archetype group onto a new "Overfit detection" line carrying its native review vocabulary, since [Proposal 017](proposals/017-s17-renaissance-backtesting-quality-lift.md)'s rewrite forbids the IC/decay/capacity vocab in `renaissance_backtesting.yml`. Item B (optional, approved): `agents/gs_compliance.yml` example-fix string generalised from the Portugal-language literal "EN/PT" to "the investor's jurisdiction-required language(s)". User-requested Isolated-Challenger pass (R1 `flawed`, L1–L10) absorbed pre-commit; /code-reviewer APPROVE on the persona file (AGENTS.md prose-exempt). | Done | Carries one out-of-scope follow-up: `dimensional_factor_backtester` is itself a backtester loosely grouped under "Quant signal" — a pre-existing imperfection (not an 017/018 contradiction), candidate for a future archetype-list completeness review. | Yes — narrative-doc edit + one persona-prompt example string; no operative persona fields touched. |
| S-21 | **IBKR read-only snapshot mode** (RE-SCOPED 2026-06-04) — add a `--snapshot` path to `scripts/reconcile_ibkr.py` pulling live positions + cash (read-only, non-default clientId), run at War Room **start** (verify ledger vs broker) and **finish** (reconcile fills). **Verification gate = the first operator-approved live `--snapshot` run** — the residual design items are runtime-verifiable-only against real TWS output and cannot be closed by a static review round: marks in a multi-account login (honest `n/a` degraded mode, since `portfolio()` is single-account-only), base-currency-row resolution (`currency=="BASE"` else account base ccy, flag-if-zero-match), and account resolution via `ib.managedAccounts()`. Split out of [Proposal 026](proposals/026-s23-isin-anchored-pricing.md) (S-23 shipped; S-21 deferred). | Next dedicated `--snapshot` proposal, gated on a live TWS run. | Confined the entire R1+R2 §4b Challenger structural residue (live-IBKR currency strings / multi-account marks). Carries forward the verified `ib_insync` 0.9.86 pre-flight (`reqContractDetails→List`; `secIdList` may-be-empty; `PortfolioItem` marks; `accountValues` multi-account-safe ≤50) + the defensive designs + the L7/L10 cross-check absorptions. | Yes — additive read-only script mode; no framework files change. |
| S-22 | **HTTP-cache TTL / `--no-cache` on macro fetch** ✅ — **DONE 2026-06-03 via [Proposal 023](proposals/023-s22-http-cache-ttl.md)**: gave `scripts/data/http_client.py`'s on-disk cache a per-series freshness gate (`get(max_age_days=…)` + `_cache_fresh`) keyed to each series' **`amber_age_days`** in `config/gates.yml` (threaded from `cli._fetch_all` through the keyword-only-no-default `DataProvider.fetch` signature into `get()`), plus a manual **`--no-cache`** flag on `fetch` (reuses the existing `skip_cache` plumbing). **Bypass-on-expiry, never evict**: an over-age entry is re-fetched on the normal path but preserved on disk and returned as the tagged-STALE DDP step-2 fallback (`RISK_FRAMEWORK.md:286`) if the re-fetch fails; the 429-over-cap path was rerouted (`last_exc`+`break`) to converge on the same post-loop fallback decision. Amber (not red) chosen so cache-age contribution stays inside the GREEN band. New `tests/test_http_client.py` (5 tests, real `get()` path, monkeypatched clock+network) + 11 passed total. Challenger R1 (flawed, 11 items) + bounded delta-R3 (flawed, 10 items) converged; /code-reviewer APPROVE WITH NOTES (4 NOTE-level). | Done | — | Yes — all local code; FULLY REVERSIBLE. |
| S-23 | **ISIN-anchored instrument pricing; retire bare-ticker yfinance lookups** ✅ — **DONE 2026-06-04 via [Proposal 026](proposals/026-s23-isin-anchored-pricing.md)** (the safety-fix half of the original S-21+S-23 bundle; split after two §4b Challenger rounds confined all structural residue to S-21's live-IBKR surface): made the **tracked framework** carry portable instrument identity so every public cloner is protected with `yfinance` alone. `_parse_open_holdings` threads the ledger ISIN (col 1) into each position; `_fetch_live_prices` adds a fail-safe **bare-ticker guard** that skips a non-US-ISIN ticker carrying no exchange suffix with a loud warning (the `DFEN`→US-Direxion-3× collision), rather than mis-pricing; `_patch_summary` annotates a guard-skipped mark as `(partial: n/m marks)` so a skip can't silently understate P&L; the tracked `PORTFOLIO.template.md` documents the exchange-qualified-ticker + ISIN contract; the live ledger's bare holding was corrected to its exchange-qualified form. Guard is a backstop to the primary qualified-ticker contract (correctly inert on clean live data; synthetic-fixture-tested). 6 new tests (guard real-entry-point + end-to-end ISIN-thread + partial-mark); 97 passed / 1 skipped. Challenger R1=14→R2=7 (all verified on disk; one structural claim refuted); /code-reviewer APPROVE WITH NOTES. | Done | — | Yes — helper hardening + tracked doc + data correction; FULLY REVERSIBLE; no broker call surface (that is S-21). |
| S-24 | **Wire the 5 manual deployment gates into the data pipeline** — 5 of 8 gates (hormuz, brent, ecb-decision, tariff, equity-vs-50wMA) are `unavailable` in the FRED/ECB pipeline → scored RED-by-omission → a permanently misleading headline `Data_Confidence`/`Market_Risk` RED; the orchestrator hand-sources them every session. Also pull the central-bank meeting calendar to stop hand-entered event-date drift. | When the data layer is next extended (advances the open P-07 streetlight-effect item). | Two of the five (commodity price, equity-vs-MA) are market-data-fetchable; the categorical/geopolitical gates need a structured manual-input slot rather than silent RED. | Yes — additive market-data provider + a structured manual-gate input slot. |
| S-25 | **Surface stale/wrong core-macro series in the gate_eval headline** — a core macro series can be both stale and materially wrong while only denting per-series staleness (it is not one of the 8 gates), so it stays invisible at the regime-analysis layer (observed in #3: the euro-area inflation series read months-stale and well below the live print). | Bundle with S-24. | Investigate why the ECB SDMX inflation fetch returns a months-old vintage; promote any STALE/RED core-inflation series to the gate_eval headline output. | Yes — provider investigation + a reporting change. |
| S-26 | **Atomic HTTP-cache write (tmp-write + rename)** — `scripts/data/http_client.py:124` writes the cache via a direct `cache_path.write_text(body)`; a crash mid-write can leave a truncated cache file. Pre-existing, but [Proposal 023](proposals/023-s22-http-cache-ttl.md) (S-22) widened the exposure by adding a read-on-failure DDP fallback path that now also reads the cache on the failure branch. Surfaced as /code-reviewer finding #4 (NOTE) during the S-22 close-out. | Next `http_client.py` edit, OR first observed truncated-cache incident. | Mirror the atomic pattern already used by the snapshot writer at `cli.py:122-130` (write to a `.tmp` in the same dir, then `os.replace`). | Yes — localised write-path hardening; no signature change. |
| S-27 | **Reconcile Summary-label reconciliation** ✅ — **DONE 2026-06-10 via [Proposal 028](proposals/028-reconcile-summary-contract-currency.md)**: `_patch_summary` anchored on `… (post-fill)` labels that fully mismatched the tracked template (0/5) and *partially* mismatched the live ledger (3/5; Cash + Invested missed) — so a real reconcile produced a PARTIAL patch (fresh + stale rows mixed), not the total no-op the original wording implied; [Proposal 025](proposals/025-reconcile-column-index-foundation-fix.md) had made it **loud** but unfixed. Fixed via a canonical Summary contract reconciled across four surfaces (script anchors ↔ template ↔ live ledger ↔ fixture): anchors renamed to the live labels (`Cash`, `Invested (MTM)`, `Equity exposure (% NAV)` literal) — live ledger already conformant per the floor audit, so **no live edit**; patching section-scoped to `## Portfolio Summary`; the 025 warning reworked into a derived-count graduated full/partial/no-op message (no hardcoded `/5`, `pending L14` dropped); template brought to the canonical CORE-6 placeholders. Converged over 3 isolated-Challenger rounds (20→15→5, R3 `sound`). | Done | Surfaced as Challenger L14 during the 025 close-out. | Yes — 4-surface reconciliation; live ledger already canonical (no edit). |
| S-28 | **Reconcile currency-aware pricing** ✅ — **DONE 2026-06-10 via [Proposal 028](proposals/028-reconcile-summary-contract-currency.md)** (trigger pulled forward by operator-confirmed bundling with S-27): `_fetch_live_prices` fed a native-venue price into the base-currency unrealised P&L. Fixed **flag-only** — a venue-currency check (`fast_info.currency`) prices a mark only when the venue confirms the base currency; any non-base/unknown venue is flagged-and-omitted with a loud warning (skip-don't-misprice), never mispriced. FX **conversion** was deferred (unsound while the write-side entry-cost leg is currency-unaware) → banked as **S-32**. | Done | Surfaced as Challenger L6 during the 025 close-out. | Yes — flag-only venue-currency check; convert-mode → S-32. |
| S-29 | **Global ISIN-anchored key-free multi-source pricing** — resolve ISIN→best quote across a ranked chain of key-free sources (yfinance → Stooq → …; OpenFIGI for ISIN→ticker), each staleness-checked (reuse the S-22 vintage discipline at the equity layer) and reliability-ranked; market-agnostic for public users. EODHD/Alpha Vantage stay key-required local-enhancement tier. The *coverage* build that complements [Proposal 026](proposals/026-s23-isin-anchored-pricing.md)'s *safety* fix (026 never mis-prices — thin-coverage instruments get an honest `n/a`; S-29 widens what resolves). | Future build; needs a multi-market coverage investigation (US/XETRA/LSE/TSX/ASX/NSE-BSE/B3/TSE) first. | Operator ruled SHIP SEPARATELY 2026-06-04 — global coverage is a distinct, likely-HEAVY build that must not be folded into the safety fix; connects to S-28. | Yes — ranked key-free source chain + OpenFIGI resolver + per-source staleness/reliability ranking. |

| S-30 | **Mechanical pre-commit sanitisation guard** ✅ — **DONE 2026-06-09 via [Proposal 027](proposals/027-s30-precommit-sanitisation-guard.md)**: the mechanical forward-prevention for the sanitisation rule that had been noted-but-unenforced (Proposal 018 Status Log) and recurred **N≥2** (the [Proposal 026](proposals/026-s23-isin-anchored-pricing.md) test leaked real qty/price/cost + the live NAV into a public tracked test — caught by steering, not tooling). Implemented: a tracked `scripts/hooks/pre-commit` shim installed cloner-portably by `init_workspace.install_hook()` (idempotent, never clobbers a foreign hook, zero new dependency), reading the gitignored `local/` ledger at runtime (literals never enter agent context, PIN 5) and **blocking the commit** when an ISIN / exchange-qualified ticker / ledger-numeric value appears in the staged tracked diff. Numeric matching = **literal-side variant-enumeration** (plain / comma-grouped-US / EU-locale, rendered from the token string so trailing zeros survive) + **raw-diff digit-lookaround search** (`re.escape`d, with diff-side ISO-date exclusion — Amendment 2) → symmetry-class eliminated, **fail-CLOSED**; **≥3-sig-fig decimal floor (Amendment 3)**; parsed-canonical allowlist (`config/sanitisation_allowlist.yml`, PIN-3); no-ops for a fresh cloner. Right-sized as a safety net (named bounded residuals), not a perfect redactor. Converged over 4 isolated-Challenger rounds (a two-sided-normaliser spiral, R4: 5 structural, was scope-reduced via Amendment 1 to one-sided enumeration); self-gate dogfooding then caught a real Challenger-surfaced value mistakenly carried into the docstring (scrubbed) + two benign over-block classes (Amendments 2 & 3). /code-reviewer APPROVE WITH NOTES; 5 runtime observations + a clean self-gate (commit passes, no `--no-verify`); 13 new tests, 110 passed / 1 skipped. | Done | Manual sanitisation review is not reliably catching investor-private literals before they reach tracked files; a deterministic gate is needed. SHA/allowlist config = parsed-canonical, not raw bytes. | Yes — pre-commit hook + an allowlist config; no framework-runtime change. |
| S-31 | **Sanitisation-guard precision follow-ups** (from [Proposal 027](proposals/027-s30-precommit-sanitisation-guard.md) /code-reviewer NOTEs) — (1) tighten ISIN / exchange-qualified-ticker matching in `find_leaks` to an alpha-aware boundary (`\b` / `[^\w]`) instead of the shared numeric digit-lookaround, avoiding a rare fail-closed over-block when an identifier abuts a longer alphanumeric run; (2) make `staged_added_text()` fail-CLOSED on a `git` error (currently returns `""` → would pass; the hook only runs under git so the exposure is theoretical, but fail-closed matches the guard's posture); (3) **workflow rule surfaced by dogfooding** — tracked design docs / proposals / CHANGELOG / code docstrings must use **placeholders**, never concrete economics-shaped numbers (the guard cannot distinguish demo from real, so a concrete demo value either collides with the real ledger or risks being a real value); concrete values live only in `tests/`, chosen hook-verified non-colliding. | Next `sanitisation_guard.py` edit (1,2); (3) is a documentation-discipline rule to fold into the proposal/CHANGELOG authoring convention. | Both NOTEs fail-closed, non-blocking at ship; (3) banked per steering 2026-06-09 after the self-gate caught a real value + benign over-blocks in this very proposal's prose. | Yes — localised matching / error-path hardening + a doc-authoring convention; no contract change. |
| S-32 | **Currency-aware P&L convert-mode + write-side persistence** — true FX conversion of non-base marks (a guarded FX-rate fetch with NaN/finiteness checks + minor-unit handling such as `GBp` pence→pounds) shipped **together** with currency-aware entry-cost persistence on the WRITE side (`_patch_holdings_row` writes a `€`-labelled amount unconditionally on a native fill), so both unrealised-P&L legs convert consistently. [Proposal 028](proposals/028-reconcile-summary-contract-currency.md) shipped S-28 **flag-only** because converting only the live leg while the cost leg stays native-magnitude is worse than omitting — this is the deferred convert build. | When a non-base-currency-venue holding is actually added, OR bundled with the S-21 `--snapshot` build; connects to S-29 (multi-source pricing). | Banked from Proposal 028 §F2+F4 (Challenger R1 L7/L8/L9 + R2 L7). | Yes — guarded FX-rate fetch + write-side currency persistence + minor-unit handling. |

> **Sanitisation audit note (2026-06-04, Block B accepted):** real position economics / NAV / contribution figures already exist in *committed* public history — the `tests/fixtures/portfolio_10col.md` VWCE economics (Proposal 025), the 024/025 proposal bodies, and the `backtesting/*` session files (ISIN `IE000YYE6WK5`, `€2,400` contribution cadence). The operator ruled **accept-the-exposure, no history rewrite** (consistent with the Proposal 018 Status Log precedent for already-public non-personal residuals). Recorded for audit; **no action**. Forward prevention is tracked as **S-30**.

> **Foundation note (2026-06-04):** the `scripts/reconcile_ibkr.py` Holdings parse/patch foundation was corrected via [Proposal 025](proposals/025-reconcile-column-index-foundation-fix.md) (the parser/patcher were on a pre-ISIN 9-column layout while the ledger/template have carried a 10th ISIN column since the initial commit → parser returned nothing, patcher was a silent no-op). **S-23** (ISIN-anchored pricing + bare-ticker guard) then shipped on the sound base via [Proposal 026](proposals/026-s23-isin-anchored-pricing.md); **S-21** (read-only `--snapshot`) was split out of 026 and remains **open**, re-scoped with its verification gate = the first live `--snapshot` run. Supersession/split trail: [Proposal 024](proposals/024-isin-anchored-pricing-ibkr-snapshot.md) (bundle, SUPERSEDED) → 025 (foundation) → 026 (S-23 ship, S-21 split).

---


## Architecture Decisions Log

> Architectural decisions (with persona attribution) are recorded in [CHANGELOG.md](CHANGELOG.md) under per-date `### Decisions` subsections.

---

## Known Issues

| Issue | Status | Reference |
|---|---|---|
| Gold ETC Portuguese tax classification uncertain | Open | `docs/COMPLIANCE.md` — confirm with contabilista whether physically-backed ETCs are Category B or G |
| Micro-NAV framework overrides | Active | `docs/RISK_FRAMEWORK.md` §Micro-NAV Overrides — standard rules phase in at €2,000 NAV |
| MCP Elicitation not yet available | Deferred | Review each session whether Claude Pro supports structured pause points |
| Karpathy Loop for strategy iteration | Backlog (Tier 3) | Agent proposes strategy tweaks, runs backtest, keeps improvements. Requires backtesting engine. See [github.com/karpathy/autoresearch](https://github.com/karpathy/autoresearch) and ATLAS (trading adaptation). |
| ReasoningBank (Ruflo-inspired) | Backlog (Tier 3) | Store successful War Room reasoning patterns for future sessions. Lighter than full Ruflo. See [github.com/ruvnet/ruflo](https://github.com/ruvnet/ruflo). |
| Prediction tracking | Backlog (Tier 2) | Log each session's macro predictions and trade theses. Compare prediction vs actual next session. Reveals systematic biases. |
| Agent calibration scores | Backlog (Tier 2) | Derive from `local/AGENT_PERFORMANCE.md` after 12 sessions. Weight Strike Team selection toward high-signal agents. |
| Gold decorrelator thesis broken | Open — requires re-proposal | Post-mortem: gold fell 12% alongside equities in March/April 2026. Decorrelation failed. Must be re-proposed under Thesis Validation Protocol before re-entry. |
| yfinance Series format error | ✅ Closed 2026-05-15 | Fresh audit confirmed engine path (`backtesting/engine/data.py:152-159`) has always carried the `hasattr(val,"iloc")` fallback (since `fd4d036`). BT #12 origin was agent-improvised code via `Task()` sub-agents — covered structurally by engine-first workflow rule. No code fix needed. |
| FRED API key exposure (2026-05-16 transcript leak) | ✅ Closed by decision 2026-05-20 | Key was leaked into a session transcript via a `diff .env` call. User accepts the exposure — FRED keys are free and read-only (macro data only). Rotation declined. Recorded here so the action stops resurfacing in reconstruction/retro passes. |

---

## Session Log

> Framework change history lives in [CHANGELOG.md](CHANGELOG.md). This file is forward-looking only.
