#!/usr/bin/env python3
"""Git pre-commit hook entry — delegates to the importable guard module.
Installed into .git/hooks/pre-commit by scripts/init_workspace.install_hook()."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from sanitisation_guard import main

if __name__ == "__main__":
    sys.exit(main())
